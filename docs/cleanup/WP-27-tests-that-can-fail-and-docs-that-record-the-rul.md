# WP-27 · Tests that can fail, and docs that record the rules

Part of the [Atlas cleanup list](README.md) — **second pass** (2026-09-26).

**Goal:** The secrecy suite and both browser suites should fail when the rule a step names breaks, and the fixture and sample world should be rebuildable from the repo. CLAUDE.md and the cleanup list itself should record the rules the code enforces.

**Do after:** [WP-08](WP-08-docs-config-and-comments-tell-the-truth.md)

**Notes:** B118 corrects C071's fix: a fixture rebuilt from fixture.sql's contract would be blinder than today's, because the contract leaves out 'Interlude Ghost'. Do B118 inside WP-08's C071 work. B119 should land before or together with B071 (WP-01) rewriting the window clamps, because today's test cannot tell whether any clamping happens. B120 tests the rules added since 2026-08-20: DM notes, map notes, stance, partyTrail, the lantern prune and a positive /locate; pair its /nodes checks with B010's, and break-test every new assertion by deleting the rule it guards. B121, B122 and C102 fix the e2e step assertions, which P025's exit-code fix alone will not make fail. P113 commits a seed for the sample template, a sibling of C071. C103 adds the rules CLAUDE.md never states, alongside O001. O028 fixes two chip comments that D008 says are gone. C101 must be applied to WP-19's text before anyone works C048: its class rename would undo 1224c84 and let ad-blockers hide the Share button again.

## Checklist

- [ ] **B118** · medium · s · The secrecy suite can't catch players reading the hidden past: no documented fixture row lives in the Hidden Era, so dropping player_visible from share.js leaves all 17 tests green
- [ ] **B119** · medium · s · 'windowed lifespans are clamped to the revealed envelope' only asserts values no clamp touches: every future-trimming clamp in share.js can be deleted and the suite stays green
- [ ] **B120** · medium · m · No API test covers any secrecy rule share.js gained after the fixture (2026-08-20): DM notes, map notes, stance, the party trail's reachable-map filter, the lantern prune, outlines. /locate has no positive test, so a locate that always 404s passes
- [ ] **B121** · medium · s · player.mjs steps stay green when what they name breaks: the era-bar check passes through the footprint step's side effect, and '⬆ returns' passes on the dead-link screen
- [ ] **B122** · medium · s · dm.mjs steps stay green when what they name breaks: the freehand region check is satisfied by the stale, already-deleted first region, and the double-click guard passes when the click never reaches the pin
- [ ] **C101** · medium · xs · C048's fix renames invite* back to share*, which undoes 1224c84, the fix that stopped ad-blockers hiding the DM's Share button
- [ ] **C102** · low · s · player.mjs can't pass on the repo's own proving ground (world 30): the party step needs the party's canon footstep on the ROOT map and a party text over 20 characters
- [ ] **C103** · low · s · CLAUDE.md never states several rules the code enforces: the Spellforge frame exception for /p, map-note and stance secrecy, the lantern's rules, the sample-template clone path, undo tombstones and guest accounts
- [ ] **O028** · low · xs · extends D008/O009: two comments from the removed 'party is at' chip and exit marker are still in the repo, although D008 says nothing of the chip is left
- [ ] **P113** · low · s · 'Begin from the sample world' exists only as a production row: a fresh deploy never offers it, and nothing in the repo can rebuild it

## Items

### B118 · The secrecy suite can't catch players reading the hidden past: no documented fixture row lives in the Hidden Era, so dropping player_visible from share.js leaves all 17 tests green

Broken · medium · effort s · second pass, found by `suites`

- **Where:** server/test/share-live.test.js:57-60 and :104-109; server/test/fixture.sql:6-16 (the contract omits node 114 'Interlude Ghost'); server/routes/share.js:34-37 (allowedTime) and :138-140 (window envelope)
- **What happens:** I rebuilt the Secrecy Fixture locally from fixture.sql's contract plus the ids and payloads the live fixture returns: private Postgres, the repo's schema.sql through applySchema, and a COPY of share.js mounted on /api/share. The repo's own share-live.test.js passes 17/17 against it, as production does. Then I mutated the copy. (1) Removing `player_visible` from allowedTime (share.js:35), so a hidden era's moment is honoured: 17/17 green. (2) Removing it from the window-envelope query (share.js:139): 17/17 green. (3) allowedTime honouring ANY past t: 17/17 green. The t=45 test compares two moments where every documented row looks the same: Open Landmark always; Brief Fair ended at 40; Future Thing starts at 80; the timed backdrop from 40 is active at both 45 and canon 50. Live, /maps/60?t=35 and ?t=45 both return exactly the canon payload. Only one row could make these checks bite: 'Interlude Ghost', named in the bad-list at :107 and added in c28e28f as 'a node alive only BETWEEN revealed eras'. It is missing from fixture.sql's contract, and its lifespan decides the result. Seeded at 32-38 ('between' the eras, as its name suggests) or left out (a contract-only rebuild), all three mutants stay green. Only at 42-48, inside the Hidden Era, does the t=45 test go red.
- **Why it matters:** The rule in CLAUDE.md and share.js:26-28 (a ?t= moment outside a player_visible era silently resolves to canon, and the window envelope uses only revealed eras) has a fixture row that exists only inside the Hidden Era, plus assertions that fail when the rule is removed. fixture.sql's contract lists every row the tests name.
- **Fix:** Add to the fixture and to fixture.sql's contract a shared node with a shared root placement alive only 42-48 ('Hidden Era Secret'), plus a node_fact 42-48 on Open Landmark ('hidden era text'). Neither row changes any existing assertion: both are dead at 20, at 50 and inside the [10,30]∪[50,50] envelope. Assert that /maps/60?t=45 titles == ['Open Landmark'], that /nodes/97?t=45 body == 'base text', and that the ?window=1 titles exclude 'Hidden Era Secret'. Write node 114 Interlude Ghost's real lifespan into the contract. Then break-test the suite: remove player_visible from share.js:35 and :139 and confirm each goes red.
- **Repro:** In lanes/suites/sb: node setup.cjs 32,38 && node mutate.cjs 32,38 'hidden eras' → 'MISSED [allowedTime counts hidden eras (player_visible dropped)] pass=17' and 'MISSED [window envelope counts hidden eras …] pass=17'. node mutate.cjs 1000,1000 'hidden eras' (ghost effectively absent) → MISSED ×2. node mutate.cjs 42,48 'hidden eras' → CAUGHT.
- **Evidence:** lanes/suites/sb/mut-ghost-gap.out, mut-contract-only.out, mut-ghost-hidden.out (mutants in sb/mutate.cjs, seed in sb/seed.sql); lanes/suites/fxprobe.out: 't 35 ["Open Landmark"] bd …fixture-a.svg' and 't 45 ["Open Landmark"] bd …fixture-a.svg', identical to canon; git log -S 'Interlude Ghost' → only c28e28f (the test), never fixture.sql.
- **Re-proved:** Re-proved independently. I built my own fixture from fixture.sql's contract only (a private embedded Postgres, the repo's schema through applySchema, and a copy of share.js mounted on /api/share; files in lanes/verify-suites-r0: seed.cjs, mut.cjs, srv/). The repo's share-live.test.js passes 17/17 on it. Mutants: A (player_visible dropped from allowedTime, share.js:35), B (dropped from the window-envelope query, :139) and C (allowedTime honours any past t). All three stay 17/17 green with no ghost row (mut-none.out) and with an 'Interlude Ghost' at 32-38 (mut-3238.out). With the ghost at 42-48…
- **New versus the first pass:** Verified directly: server/test/fixture.sql's contract (lines 6-14) omits any row for 'Interlude Ghost', though share-live.test.js:107 names it in the windowed bad-list and c28e28f's commit describes it as living only between revealed eras. Confirmed share.js:35 (allowedTime) and :139 (window envelope) both filter on player_visible exactly as cited. The lane's mutation harness (sb/mutate.cjs) runs…
- **Corrects the fix in:** [C071](WP-08-docs-config-and-comments-tell-the-truth.md) — A seed rebuilt from the contract as written makes the suite blinder: the contract leaves out 'Interlude Ghost', and with no row inside the Hidden Era, removing player_visible from share.js leaves all 17 tests green. Add a 42-48 row and Ghost's real lifespan to the contract.
- **Related:** [D017](WP-07-delete-the-dead-code.md) — Moving the four ?t= tests to ?window=1&t= keeps them blind to hidden-era leaks until the fixture has a row inside the Hidden Era (B118).

### B119 · 'windowed lifespans are clamped to the revealed envelope' only asserts values no clamp touches: every future-trimming clamp in share.js can be deleted and the suite stays green

Broken · medium · effort s · second pass, found by `suites`

- **Where:** server/test/share-live.test.js:111-119; server/routes/share.js:191-195 (placement clamp), :214-215 (backdrop clamp), :250-251 (partyTrail clamp), :110-114 (/world eras: start<=canon filter and Math.min end clamp)
- **What happens:** The test asserts Brief Fair 20/40 and Open Landmark null/null. With lo=10 and canon=50, `(20 < lo) ? null : 20` and `(40 > canon) ? null : 40` give back the raw values, so the test cannot tell whether clamping happens at all. Mutants on the local share.js copy: placement clamps replaced by raw start_time/end_time → 17/17 green. Backdrop clamps removed → 17/17 green. /world eras listed without `start_time <= canon` and without the end clamp → 17/17 green. No fixture row starts before the first revealed era, ends after canon while visible, or is a player-visible era that begins after canon. So the rule in the comment at share.js:191 ('nothing before the first open era or past canon leaks') is untested: for example, a placement 25-70 would ship end:70, the future, to players.
- **Why it matters:** The test's named rule has a fixture row that exercises it, and deleting the clamp turns the test red.
- **Fix:** Put the clamp probes on a separate player-reachable map, e.g. the interior of a new shared node on root, and assert there. Adding them to root would break existing root assertions: a 25-70 placement is present at canon 50 and at t=99, a 5-25 placement is present at t=20, and a 45-70 backdrop becomes the active canon backdrop and makes backdrops.length 2. On that map, a shared placement 25-70 must give window end === null, and JSON.stringify(payload) must not contain 70. A placement 5-25 must give start === null. A timed backdrop 45-70 must give end === null. At world level, add a player_visible era 60-70 and assert /world eras still equal ['Open Era']. Break-test each assertion by deleting its clamp.
- **Repro:** In lanes/suites/sb: node mutate.cjs 32,38 clamps → 'MISSED [windowed placement clamps deleted] pass=17', 'MISSED [windowed backdrop clamps deleted] pass=17'. node mutate.cjs 32,38 '/world' → 'MISSED [/world era end not clamped to canon, future revealed eras listed] pass=17'.
- **Evidence:** lanes/suites/sb/mut-ghost-gap.out and mut-contract-only.out (both MISSED); live windowed payload (fxprobe.out): Brief Fair s:20 e:40, Open Landmark null/null, backdrop start 40 end null. These are the only rows the test reads.
- **Re-proved:** Re-proved on my own contract-only sandbox. Mutant D (the windowed placement clamps at share.js:193-194 replaced by raw start/end), mutant E (the backdrop clamps at :214-215 removed), mutant F (/world eras without `start_time <= canon` and without the Math.min end clamp) and mutant G (the partyTrail clamps removed) all stay 17/17 green, with or without an Interlude Ghost row (mut-none.out, mut-3238.out, mut-4248.out). D and E are missed whatever production holds. Brief Fair is 20/40 raw (live windowed s:20 e:40, and with lo=10 and canon=50 the clamp is a no-op). Open Landmark is null/null. The…
- **New versus the first pass:** Verified: the windowed-clamp test (share-live.test.js:111-119) asserts Brief Fair 20/40 and Open Landmark null/null, both of which are the raw/no-op values given lo=10, canon=50 — confirmed by reading the fixture data (Brief Fair lifespan 20-40 is within [10,50]; Open Landmark is unbounded). The clamp code at share.js:191-195 (placement), 214-215 (backdrop), 250-251 (partyTrail), and 110-114 (/wo…
- **Related:** [B071](WP-01-close-the-leaks-in-the-share-api-and-image-ids.md) — The windowed-clamp test asserts only values that no clamp touches, so it cannot tell whether your snapping works. Add clamp probes on a separate map and break-test them before changing share.js.

### B120 · No API test covers any secrecy rule share.js gained after the fixture (2026-08-20): DM notes, map notes, stance, the party trail's reachable-map filter, the lantern prune, outlines. /locate has no positive test, so a locate that always 404s passes

Broken · medium · effort m · second pass, found by `suites` (+1 other lane)

- **Where:** server/test/share-live.test.js (no test for these rules; the only locate test, :92-95, expects 404); server/routes/share.js:73-102 (spotlightTrail, prune at :98), :232-254 (partyTrail, reachable filter at :243-247), :302-305 (node select), :146-148 (map select), :337-352 (locate); rules documented at CLAUDE.md:93, :115 and schema.sql:170-181
- **What happens:** Mutants of the local share.js copy, all 17/17 green: node detail also returning n.dm_note and n.stance; the map payload returning maps.dm_note; partyTrail keeping unreachable maps and a DM-only party; spotlightTrail with its prune-at-first-secret removed; placement shape/shapeKind dropped; /locate returning 404 for every node. The only locate test (:92-95) asserts a 404, so a locate that never works passes. The fixture predates each feature: dm_note bb4f7a4 (08-25), the lantern 65119f7 (08-25), map notes and stance 61f8a14 (09-01), partyTrail da0d936 (09-25), outlines 262da55 (09-25). It has no Party (the live windowed payload has partyTrail []), no spotlight and no dm_note values. `grep -c 'dm_note\|dmNote\|stance\|spotlight\|partyTrail\|shape\|voice' server/test/share-live.test.js` → 0. The file's header (:1-3) calls it 'the security boundary of the whole app'.
- **Why it matters:** CLAUDE.md's Sharing and Timeline sections list these as server-side secrecy guarantees: dm_note is 'never selected by share.js'; partyTrail is 'reachable maps only'; the lantern is 'pruned at the first step a player may not see'. Each needs a check that fails when the rule is removed.
- **Fix:** (1) Give Open Landmark a dm_note and a stance, and root map 60 a map note. Assert that none of those strings appears in JSON.stringify of /nodes/97 or /maps/60?window=1. (2) Add a Party node (category 'party') with a footstep on a reachable map, a footstep inside Hidden Interior (map 61, behind Secret Door) and one DM-only footstep. Assert that partyTrail holds only the reachable step. Put its root-visible placement where the existing root title lists are updated, or on a separate map. (3) Test the lantern with a partial trail, so that both 'no prune' and 'always []' fail. Example: give Open Landmark a shared interior holding a DM node, spotlight that DM node, and assert spotlight deep-equals [{title:'Open Landmark', mapId:60,…}]. (4) Add a positive locate: /nodes/97/locate must return {mapId:60}. Break-test each new check by deleting the rule it guards. Drop the outline item: dm.mjs already guards it.
- **Repro:** In lanes/suites/sb: node mutate.cjs 32,38 'DM note' → MISSED ×2. node mutate.cjs 32,38 partyTrail / lantern / locate / outline → MISSED each (see mut-ghost-gap.out).
- **Evidence:** lanes/suites/sb/mut-ghost-gap.out lines 'MISSED [node detail sends the DM note and stance] pass=17', 'MISSED [map payload sends the map DM note] pass=17', 'MISSED [partyTrail: unreachable maps kept, dm party kept]', 'MISSED [lantern trail: no pruning at secret or out-of-time steps]', 'MISSED [locate always 404s]', 'MISSED [placement shape/outline dropped from payload]'; git log --format='%h %ad %s' -- server/test.
- **Re-proved:** Re-proved. In my sandbox these mutants all stay 17/17 green: H (node detail also returns n.dm_note and n.stance), I (map payload returns maps.dm_note), J (partyTrail keeps unreachable maps and a DM-only party), K (spotlightTrail prune at :98 removed) and L (locate always 404s). They are missed whatever the fixture data, because no test reads those fields. share-live.test.js never mentions dm_note, stance, spotlight or partyTrail, and its only locate test (:92-95) expects 404. Live, the fixture's windowed payload has spotlight [] and partyTrail []. The e2e suites do not cover these either: pla…
- **New versus the first pass:** Verified by reading share.js directly: the node-detail SELECT (lines 302-305) and the map SELECT (line 146) do NOT select dm_note or stance (confirmed those columns exist via schema.sql:173,177,181), so the app currently correctly withholds them — but no test in share-live.test.js checks this (grep confirms 0 hits for dm_note/stance/spotlight/partyTrail/shape/voice in the test file). CLAUDE.md ex…
- **Related:** [B010](WP-01-close-the-leaks-in-the-share-api-and-image-ids.md) — Add these alongside your /nodes 404 tests: DM notes, map notes, stance, partyTrail, the lantern prune and a positive /locate are all untested, and deleting any of those rules leaves the suite green.
- **Also found as:** "The share-API secrecy suite stopped growing on 2026-08-20: every secret-bearing field added since (…" (history)

### B121 · player.mjs steps stay green when what they name breaks: the era-bar check passes through the footprint step's side effect, and '⬆ returns' passes on the dead-link screen

Broken · medium · effort s · second pass, found by `suites`

- **Where:** e2e/player.mjs:89-95 (era bar), :69-78 (⬆ back; detach wait at :76), :50-58 (party step: .rtrail counted at :56 and never asserted at :57)
- **What happens:** I ran a copy of player.mjs (only change: a BREAK hook) against a clone of world 30 with a fresh share link. (a) Arrow keys blocked on the era bar's range input: at world 30's canon 37 the run still reads 'ok era bar scrubs into the past — 🕓 Session 1 · footstep 1 · the past'. The footprint step just before it (:80-88) already put viewT in the past, viewT survives, and the step checks only /past/. The same break with canon moved to 25 (no footprint to click first) gives 'FAIL era bar scrubs into the past — 🕓 Session 2 · footstep 6'. (b) The parent map answers 404 after the interior is entered: 'ok ⬆ returns to the parent map — …/m/663 in 258ms' while the Player View shows 'This link isn't active' (the next three steps FAIL). The step only waits for .backbtn to detach, which the dead-link and loading screens also satisfy. (c) The party step's comment (:50) promises 'the from/to trail', but .rtrail only goes into the note. The assertion is `ok && body.length > 20` (read from code; world 30 never reaches this branch with a trail, see the next finding).
- **Why it matters:** Each step asserts a state that only the named behaviour produces: the lens moved because of the key presses, the parent map is showing, and the From/Then-on links are present when the payload has a neighbour.
- **Fix:** Era bar: click ⦿ Now (or read the chip) before pressing keys, then assert the chip changed and differs from the footprint step's moment. ⬆: record the ⬆ button's label (the parent title) before clicking. Then assert `.pview .deadlink` count is 0 and `.crumbs .here` text equals that label. A URL check alone cannot catch this: the URL is already /m/<parent> when the fetch 404s. Party: when the windowed payload's partyTrail has a neighbour of the canon footstep, assert `.rtrail a` >= 1. Otherwise drop 'and the from/to trail' from the comment at :50.
- **Repro:** lanes/suites: RUNS=P0,P1,P2 node run-suites.mjs (canon 37; P1 = BREAK trail,keys; P2 = BREAK parent404). CANON=25 RUNS=P0,P1 node run-suites2.mjs. Each script clones world 30 as '[audit2] suites' and deletes it in finally.
- **Evidence:** lanes/suites/run-suites.out: P1 'ok era bar scrubs into the past — 🕓 Session 1 · footstep 1 · the past'; P2 'ok ⬆ returns to the parent map' followed by 'FAIL ghost footprints…', 'FAIL era bar present', 'FAIL no page errors — … 404'. run-suites2.out: P1 at canon 25 'FAIL era bar scrubs into the past'. e2e/breaks.log: '404 for parent map 663'.
- **Re-proved:** Proved from code, and the finder's live break-runs agree (run-suites.out P1/P2, run-suites2.out). (a) The era-bar step (player.mjs:90-95) reads .nowchip only AFTER the 12 ArrowLeft presses and passes on /past/. The step just before it (:80-88) passes only if the chip already matches /past/, and nothing between them resets viewT. So whenever the footprint step passes, the era-bar step passes even if the range ignores the keys. At canon 25, with no footprint to click first, the same key block gives FAIL. (b) Navigating to the parent does not clear `data` (PlayerView.jsx:47-61), so the loading s…
- **New versus the first pass:** Verified against the real repo e2e/player.mjs: cited line ranges (50-59 party step, 69-78 back-button step with the detach wait at 76, 89-96 era-bar step) match the actual repository file exactly (confirmed via diff — the lane's copy differs from the repo only by one injected BREAK-hook line). The mutation evidence is genuine live-browser proof, not just code reading: run-suites.out (canon 37, fo…
- **Related:** [P025](WP-02-protect-the-live-campaign-data-undo-clone-and-ba.md) — A non-zero exit code is not enough: player.mjs's era-bar and ⬆ back steps pass even when what they name is broken (B121).

### B122 · dm.mjs steps stay green when what they name breaks: the freehand region check is satisfied by the stale, already-deleted first region, and the double-click guard passes when the click never reaches the pin

Broken · medium · effort s · second pass, found by `suites`

- **Where:** e2e/dm.mjs:139-158 (freehand; stale state from the API-only cleanup at :132-138), :66-75 (double-click guard; force:true at :72), :48-52 (tick label), :57-64 ('Then on to')
- **What happens:** A copy of dm.mjs on a clone of world 30 (my config, my share link) passed 26/26 at baseline in 23 s. Break run: (1) The freehand's Enter was swallowed, so no region was created, yet the run printed 'ok Enter closes the traced loop into a region — 1 → 1 region(s), 1 selected', the same note as the baseline run. The first outlined place was deleted through the API (:132-138) without refreshing the page, so it is still drawn and still selected (startOutline, AtlasWorkspace.jsx:319-324, never clears selId). Only the later cleanup step ('0 removed') went red. (2) The pin's dblclick was swallowed before React saw it, and the run still printed 'ok double-click on a pin without an interior stays put — no navigation'. The step clicks with force:true and compares only the URL; it never checks the 'has no interior' flash (AtlasWorkspace.jsx:379) or that no interior was created. (3) Today on world 30 the tick step passes with 'Session 3 · footstep 8 → Behind the Screen · footstep 10', which is the DM-only era's name (B057's bug), because it only tests /footstep/. (4) 'Then on to' checks the URL and the label but not that the Party is still selected, so B022 passes.
- **Why it matters:** Each step fails when its behaviour is broken: a NEW region exists after Enter; the double-click reached the pin and was refused; the label is the session label; the Party's reader follows it.
- **Fix:** Freehand: after Enter, assert that `.atlas .ovtx` is 0 and that the selected polygon's point count equals the traced corner count. Or GET /api/atlas/maps/:id and assert that a shaped placement exists: the first one was already deleted, so any shaped placement is the new one. Clearing the stale selection alone is not enough. Double-click: assert that `.aflash` contains 'has no interior' (the baseline shows it), and optionally drop force:true. Tick: assert that the label is a session label, e.g. /^Session \d+ · footstep \d+$/ (after B057 is fixed it should read 'Session 1 · footstep 1'). 'Then on to': assert that `.reader h3` reads 'The Party' after the link. That step goes green once B022's pendingSelect fix lands.
- **Repro:** lanes/suites: RUNS=D0,D1 node run-suites.mjs (D1 = BREAK enter2,dblmiss; the world is deleted in finally).
- **Evidence:** lanes/suites/run-suites.out: D0 and D1 both 'ok Enter closes the traced loop into a region — 1 → 1 region(s), 1 selected'; D1 'FAIL the traced test place is removed again — 0 removed'. e2e/breaks.log: '[break] enter2 swallowed', '[break] pin dblclick swallowed'. D0 'ok a tick moves the lens (era-relative label) — Session 3 · footstep 8 → Behind the Screen · footstep 10'.
- **Re-proved:** I re-proved this myself on my own clone of world 30 (world 225, deleted in finally, DELETE 200), using copies of the repo's dm.mjs plus a browser-only hook (lanes/verify-suites-r1/run.out, e2e/hook.log). Baseline D0 was 26/26. Break D1 was 25/1. (1) Freehand. In the baseline, Enter #2 POSTs a new node (placementId 3189). The selected region then has 16 points (the traced loop) and ovtx=0. In the break run, Enter #2 is swallowed: no POST happens, ovtx stays 24 (still drawing), and the selected region is the stale 3-point triangle whose node the suite had already DELETEd over the API at dm.mjs:…
- **New versus the first pass:** Verified against e2e/dm.mjs (all cited lines match) and against the lane's actual run-suites.out/breaks.log: D0 and D1 (BREAK=enter2,dblmiss) print identical 'ok' for the freehand-Enter and double-click steps despite the break log confirming both interactions were swallowed, and the next cleanup step correctly FAILs with '0 removed' proving no region was created. Distinct root cause from P025 (ex…
- **Related:** [P025](WP-02-protect-the-live-campaign-data-undo-clone-and-ba.md) — dm.mjs's freehand and double-click steps also pass when broken. The freehand step passes through a stale selection left by the API-only cleanup at :132-138.

### C101 · C048's fix renames invite* back to share*, which undoes 1224c84, the fix that stopped ad-blockers hiding the DM's Share button

Confusing · medium · effort xs · second pass, found by `history`

- **Where:** docs/cleanup/WP-19-one-word-for-one-thing.md:68 (C048 Fix) and :9 (package Notes: 'Rename code identifiers (… invite vs share) in WP-20'); client/src/pages/AtlasWorkspace.jsx:964-969; client/src/styles/atlas.scss:134-145
- **What happens:** C048 lists the invite* class names as an inconsistency to clean up: 'rename the invite* classes to share* in AtlasWorkspace.jsx:964-969 and atlas.scss:135-145'. Those names were chosen on purpose. Commit 1224c84 (2026-08-21) renamed sharewrap/sharebtn/sharepop to invitewrap/invitebtn/invitepop because 'Class names like sharebtn match the cosmetic social sharing rules in common blocker lists, which hide the DM's Share button on affected browsers.' No comment in the JSX or the SCSS records this, so C048's finder and its re-prover both took the names for drift. `grep -rni 'blocker|adblock|social|1224c84' docs/cleanup/` returns nothing.
- **Why it matters:** The cleanup list never tells the next session to revert a deliberate fix. The class names say why they are not 'share*'.
- **Fix:** Edit C048's Fix so it keeps only the Dashboard badge wording change and drops the class rename. Remove 'invite vs share' from WP-19's Notes line 9. Add a one-line comment above .invitebtn (atlas.scss:135) and at AtlasWorkspace.jsx:964: 'not share*: ad-blocker social filters hide share-named elements (1224c84)'. Any later class rename (WP-18/WP-20) avoids share/social substrings.
- **Repro:** git show 1224c84 (read the message); then read WP-19 C048's Fix line.
- **Evidence:** git show 1224c84: '- .sharebtn{…}' '+ .invitebtn{…}' plus the message quoted above. WP-19:68: 'rename the invite* classes to share*'. atlas.scss:134 has only the comment '// share link (DM popover) + Player View chrome', which gives no reason for the names.
- **Re-proved:** I re-proved this myself. `git show 1224c84` (Bennett, 2026-08-21) renamed sharewrap/sharebtn/sharepop to invitewrap/invitebtn/invitepop. The commit message says 'Class names like sharebtn match the cosmetic social sharing rules in common blocker lists, which hide the DM's Share button on affected browsers.' WP-19:68 (C048 Fix) says 'rename the invite* classes to share* in AtlasWorkspace.jsx:964-969 and atlas.scss:135-145', and WP-19:9 (package Notes) also tells the reader to 'Rename code identifiers (… invite vs share) in WP-20'. WP-20 itself has no invite item. The code still carries the del…
- **New versus the first pass:** Confirmed via `git show 1224c84`: the commit message states exactly the ad-blocker rationale ('Class names like sharebtn match the cosmetic social sharing rules in common blocker lists, which hide the DM's Share button on affected browsers') and the diff renames sharebtn/sharewrap/sharepop -> invitebtn/invitewrap/invitepop. Confirmed current code still uses invite* (AtlasWorkspace.jsx:964-965, at…
- **Corrects the fix in:** [C048](WP-19-one-word-for-one-thing.md) — Do not rename invite* back to share*: commit 1224c84 chose invite* on purpose, because ad-blocker 'social sharing' filters hide share-named elements and so hid the DM's Share button. Change only the Dashboard badge, drop 'invite vs share' from WP-19's notes, and add a comment recording why.

### C102 · player.mjs can't pass on the repo's own proving ground (world 30): the party step needs the party's canon footstep on the ROOT map and a party text over 20 characters

Confusing · low · effort s · second pass, found by `suites`

- **Where:** e2e/player.mjs:18 and :51-59 (root-only party lookup, body.length > 20 at :57), :81-88 (.fstep on root); e2e/README.md:8-9; e2e/player.config.example.json:3
- **What happens:** World 30 meets README's requirement ('a party trail and at least one player-visible era'), and dm.config.json uses it. On a fresh clone of it: at its canon 37, 'FAIL a party pin is on the world map at canon — none found' (the party is inside 'The Keep — Inside' for 30-39, and the suite only looks at the root map). With canon moved to 25 (party on the root): 'FAIL the party pin opens with its footstep story — The adventurers.… trail links: 0' (a 16-character body fails body.length > 20) and 'FAIL ghost footprints are on the world map — no .fstep found'. So the party-sheet checks cannot run on world 30 at any canon. Any campaign whose party is indoors at canon, the usual mid-session state, gets the same FAIL. The local player.config.json points at a different world from dm.config.json.
- **Why it matters:** Either the suite finds the party wherever it stands at canon, or README and player.config.example.json state the exact data it needs.
- **Fix:** Get the canon footstep from the windowed payload's partyTrail (or from /world plus the root payload), navigate to its map, and test the party there. Drop the >20-character rule or seed a longer fact. Otherwise state in README and the example config: the party's canon footstep is on the root map, an earlier footstep is on the root map, and the canon fact is longer than 20 characters.
- **Repro:** lanes/suites: RUNS=P0 node run-suites.mjs (canon 37), then CANON=25 RUNS=P0 node run-suites2.mjs.
- **Evidence:** lanes/suites/run-suites.out P0: 'FAIL a party pin is on the world map at canon — none found' ({"pass":7,"fail":1}); run-suites2.out P0: 'FAIL the party pin opens with its footstep story — The adventurers.… trail links: 0', 'FAIL ghost footprints are on the world map' ({"pass":6,"fail":2}).
- **Re-proved:** I re-proved this on my own clone of world 30 (world 225, deleted in finally), using an unmodified copy of player.mjs (lanes/verify-suites-r1/run.out). The world's data: the party trail is [10-29 on root 742] and [30-39 on interior 743], canon is 37, and the party node's body is 'The adventurers.' (16 chars) with facts=[]. At canon 37 the result is 'FAIL a party pin is on the world map at canon — none found' (7/1). The party is inside the interior, and player.mjs:18/:51 only ever looks at the root map. After PATCHing canon to 25 on my clone, the result is 'FAIL the party pin opens with its foo…
- **New versus the first pass:** Verified against e2e/player.mjs (body.length>20 check, root-map-only party lookup) and e2e/README.md:8-9's stated requirement. Confirmed via the lane's actual run-suites.out/run-suites2.out that player.mjs fails on a fresh clone of world 30 (the repo's own designated proving ground, which the first pass's own appendix brief describes as satisfying README's stated requirement) at both canon 37 and…
- **Extends:** [P025](WP-02-protect-the-live-campaign-data-undo-clone-and-ba.md) — player.mjs cannot pass on world 30 at all: it looks for the Party only on the root map and requires a party text over 20 characters.

### C103 · CLAUDE.md never states several rules the code enforces: the Spellforge frame exception for /p, map-note and stance secrecy, the lantern's rules, the sample-template clone path, undo tombstones and guest accounts

Confusing · low · effort s · second pass, found by `spec`

- **Where:** CLAUDE.md:59-67 (Sharing), :89-96 (colour language and dm_note), :196-198 (CSP), measured against server/server.js:47-63 (Spellforge frame-ancestors carve-out for /p, XFO removed); server/config/schema.sql:175-181 with share.js:21/:147/:303 (maps.dm_note and nodes.stance never selected); share.js:69-101 spotlightTrail and atlas.js:115-130 (lantern); schema.sql:322-325 and atlas.js:157-173 (is_template: clonable by anyone signed in, including DM content; 50-world cap on clone only).
- **What happens:** CLAUDE.md is the file a session is told to trust over HANDOFF (HANDOFF.md:3). It documents the nodes.dm_note secrecy rule but not the identical rules for maps.dm_note and nodes.stance, which exist only in schema comments. The lantern appears only as 'the lantern glows gold' (:92), without its canon and pruning rules. The only Spellforge-specific header exception in server.js is absent from 'Sharing (Player View)'. Templates, clone, tombstone undo and guest sign-in, all live paths a DM uses, are absent; tombstones are described only in HANDOFF.md:46-47. Auth is described as 'JWT + server-side SSO' (:35, :42-43).
- **Why it matters:** Each rule a maintainer must preserve is stated where the spec discusses that area. A change such as `SELECT m.*` in share.js, tightening the CSP, or editing the clone cap should not silently break a promise that is written down only in a code comment.
- **Fix:** Add four lines to CLAUDE.md. Sharing: '/p and /p/* alone may be framed, by Spellforge only (server.js:47-63 rewrites helmet's frame-ancestors and drops XFO); the DM UI is never frameable — do not set frame-ancestors in the helmet config without updating that rewrite.' Secrecy: 'maps.dm_note and nodes.stance follow the nodes.dm_note rule: never selected by share.js.' Lantern: 'worlds.spotlight_node_id; share.js spotlightTrail resolves it at canon and stops at the first DM-only or out-of-time step.' Templates: 'worlds.is_template (no UI, set by hand on sample worlds only) makes a world clonable, DM-only content included, by any signed-in user; clone is capped at 50 owned worlds.' Leave tombstones and guest sign-in to C051/O001 and C052.
- **Repro:** Read CLAUDE.md:30-67 and 89-96 next to the cited code lines.
- **Evidence:** Code lines cited. grep of CLAUDE.md for spellforge, frame, stance (outside the Forge section), map notes, template, clone, tombstone and guest finds none of these rules.
- **Re-proved:** Re-proved each citation. (1) server.js:47-63 rewrites `frame-ancestors 'self'` to add Spellforge for /p and /p/* only, and removes X-Frame-Options. CLAUDE.md never mentions Spellforge, frames or frame-ancestors, although CLAUDE.md:196-198 documents a different helmet CSP directive (media-src). The rewrite is a string replace on helmet's default and would silently no-op if someone set frame-ancestors in the helmet config at :27-44. (2) The schema.sql:175-177 and :179-181 comments put nodes.stance and maps.dm_note under the dm_note secrecy rule. share.js never selects either column (map query :…
- **New versus the first pass:** Verified new: grepped docs/cleanup/ and read O001, C052, O017 and the appendix in full. O001 (WP-08) is exclusively about CLAUDE.md statements that are factually wrong or stale (drift); it never flags a rule that is simply absent. C052 flags README omitting guest sign-in (a different file, different fix target: 'mention it in README', not 'state the rule CLAUDE.md must preserve'). O017 is about t…
- **Related:** [O001](WP-08-docs-config-and-comments-tell-the-truth.md) — Besides fixing what is wrong in CLAUDE.md, add the rules it never states: map-note and stance secrecy, the lantern's pruning, the template clone path and the Spellforge frame exception.

### O028 · extends D008/O009: two comments from the removed 'party is at' chip and exit marker are still in the repo, although D008 says nothing of the chip is left

Obsolete · low · effort xs · second pass, found by `history`

- **Where:** server/routes/share.js:229-230
- **What happens:** D008 states 'Nothing else from the chip is left: .partychip CSS and both JSX blocks are gone.' e2e/player.mjs:97 still reads '// the trail layer and party chip exist in the DOM without throwing', above a step that only checks page errors (the chip was removed by 4a1db44, which didn't touch e2e). share.js:229-230 says partyTrail exists 'so a player's view can say where they are at any allowed moment and where they went after leaving this map'. That was the '⚑ You are at …' chip and the on-map exit marker. Today partyTrail feeds only partyNeighbors' From/Then-on-to text on the Party's sheet (PlayerView.jsx:323).
- **Why it matters:** Comments describe what exists: the suite checks for no page errors, and partyTrail feeds the Party sheet's From / Then on to links.
- **Fix:** share.js:229-230 → '// The party's footsteps across the whole world, for the Party sheet's "◂ From …" / "Then on to … ▸" links (partyNeighbors in utils/moment.js).' Keep line 231 (same envelope, reachable maps only). The player.mjs:97 half is already covered by P025.
- **Repro:** git grep -n 'party chip\|say where they' → the two lines; git show 4a1db44 --stat (no e2e or share.js change).
- **Evidence:** git grep output: 'e2e/player.mjs:97: // the trail layer and party chip exist in the DOM without throwing'; 'server/routes/share.js:229: // The party's footsteps … can say where they'.
- **Re-proved:** Kept for share.js only. Half the finding is already in their list: P025 (WP-02) says 'player.mjs:97's comment still mentions the removed party chip' and its fix reads 'Fix the player.mjs:97 comment', so that half is a duplicate and I dropped it. The share.js half is real and new. `git log -L` dates share.js:229-231 to da0d936 (2026-09-25). That was before 4c1baa7 turned the on-map 'went to' marker into text and before 4a1db44 removed the '⚑ The party is at' chip. 4a1db44 only touched CLAUDE.md, AtlasWorkspace.jsx, PlayerView.jsx and atlas.scss. The comment says partyTrail exists so 'a player'…
- **New versus the first pass:** Verified new. Confirmed both stale comments verbatim: e2e/player.mjs:97 reads '// the trail layer and party chip exist in the DOM without throwing' directly above a step that only asserts zero page errors; server/routes/share.js:229-230 reads '// The party's footsteps across the whole world, so a player's view can say where they are at any allowed moment and where they went after leaving this map…
- **Extends:** [D008](WP-07-delete-the-dead-code.md) — Something from the chip is left: the partyTrail comment at share.js:229-230 still describes the removed chip and exit marker. e2e/player.mjs:97 is the other one, and P025 covers it.

### P113 · 'Begin from the sample world' exists only as a production row: a fresh deploy never offers it, and nothing in the repo can rebuild it

Product polish · low · effort s · second pass, found by `configs`

- **Where:** server/routes/atlas.js:157-161 (GET /templates); server/config/schema.sql:322-325; client/src/pages/Dashboard.jsx:84, 272, 290-299; docs/UX-REDESIGN.md:216 (marked done)
- **What happens:** On a fresh local deploy, after /setup: GET /api/atlas/templates → {templates:[]}. 'Found your first world' opens a dialog with only Name, Chronicle and 'Found it & open the Atlas', with no sample option (screenshot). `git log --all -S 'Sunken Keep'` finds only the commit message of 6bf13db and docs/cleanup. No seed script, SQL or JSON builds the template, and no route or UI sets is_template. The first-run path UX-REDESIGN.md:216 ticks off ('pre-checked for a user's first world') therefore depends on one hand-made row in the production DB (world 27), which the e2e suites and fleet harness also clone.
- **Why it matters:** The repo can rebuild the sample world: an idempotent seed (or an exported JSON loaded through the clone code) that a fresh deploy or a DB restore can run.
- **Fix:** Commit scripts/seed-sample.js (or server/config/sample-world.json plus a loader) that builds 'The Sunken Keep — Sample' with is_template=true, if missing, through the same inserts the clone uses. Mention it in README 'Getting started'.
- **Repro:** Run the kit on a new pgdata (up.sh <dir> 55562 5162), open /setup and create the admin, then click 'Found your first world'. Or `node lanes/configs/setup-ui.mjs`.
- **Evidence:** setup-ui.mjs; screenshot lanes/configs/shots/s-create.png; curl /api/atlas/templates → {"templates":[]}; the git log searches above.
- **Re-proved:** Re-proved. On a fresh local pgdata, GET /api/atlas/templates returned {templates:[]}. The dashboard's sample option renders only when templates.length > 0 (Dashboard.jsx:272, 290-295), so a fresh deploy or restored DB never offers it. atlas.js:158-160 reads worlds WHERE is_template=true. `git grep -i is_template` finds only schema.sql:325, atlas.js:159/170 and UX-REDESIGN.md:216. There is no seed, SQL or JSON, and `git grep -i 'sunken keep'` outside docs/cleanup finds nothing. `git log --all -S 'Sunken Keep'` finds only 6dee15d (docs/cleanup), and 6bf13db mentions the seeding only in its comm…
- **New versus the first pass:** Verified via git log -S "Sunken Keep": the string appears only in commit 6bf13db's commit-message narrative ('The Sunken Keep — Sample is seeded as a teaching template'), never in any actual file diff -- confirming no seed script, SQL, or JSON ever built this row; it exists only as a hand-inserted production row. is_template is only ever read (atlas.js:159,170), defaulted false in schema.sql:325,…
- **Related:** [C027](WP-16-accounts-sign-in-and-the-dashboard.md) — On a fresh deploy there is no template row, so the sample option never appears, and nothing in the repo can rebuild 'The Sunken Keep — Sample'.
