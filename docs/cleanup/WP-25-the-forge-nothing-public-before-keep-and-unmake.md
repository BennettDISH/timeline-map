# WP-25 · The Forge: nothing public before Keep, and Unmake removes only its own

Part of the [Atlas cleanup list](README.md) — **second pass** (2026-09-26).

**Goal:** Forge output that changes existing shared things should not reach players before the DM keeps it, and Unmake should remove only what the batch made. The mind should know what players can see and should see a long campaign's recent content.

**Do after:** [WP-06](WP-06-forge-recaps-and-voice.md)

**Notes:** Needs B003 (WP-06) first: once enrich facts land, B109's leak also covers facts on shared nodes. B109 is high. Choose with Bennett between applying effects on existing things only at Keep and having share.js hide pending batch output; at minimum, fix the batch card copy and CLAUDE.md's 'born DM-only' wording. B110 extends B002: B002's confirm must count hand-made placements, Party footsteps and player markers inside Forge-built maps, plus paintings reused elsewhere, and discardBatch must skip or re-home them. B111 lets Unmake survive a removed origin map and marks asks whose target is gone. B112 and B113 are both the digest (mind.js:96-112): send visibility, mark player markers, order the caps by recency and send the Party as one entry — do them in one change together with O016's rulebook rewrite. B114 moves the lore append into SQL, which B001's client fix does not reach. B115 stops validateBatch throwing on null entries. O026, D023 and C096 are small. Borders WP-06; kept separate because these are Forge surfaces the first pass listed as not run: Unmake after the DM built on a creation, Allow after the target was deleted, and digest size.

## Checklist

- [ ] **B109** · high · m · Forge output that touches existing things reaches players as soon as a batch lands, before Keep, although the docs and the batch card say generated things stay DM-only — done c758bd4 (share.js hides everything a PENDING batch wrote — maps, links, timed backdrops, facts, and the prior art/body/standing backdrop of touched things — until Keep; the card says so; CLAUDE.md reworded; reveal listed among asks; the picker's ✦ Paint routes qualified)
- [ ] **B110** · high · m · extends B002: Unmake also deletes what the DM built on top of a creation. That covers paintings the DM reused (as node art, a base backdrop, a timed backdrop or the style anchor) and every hand-made placement, Party footstep and player marker inside a Forge-built interior. Nothing gets a tombstone — done c758bd4 (Unmake refuses with 409 `blocked` while hand-made placements/footsteps/markers stand inside its maps or the DM hung spaces on its nodes — the card names them; paintings used elsewhere are kept, the anchor only if the batch set it)
- [ ] **B111** · medium · s · Forge asks outlive their targets: an allowed move whose origin map is later removed makes Unmake fail forever with a raw foreign-key error, and a pending ask on a deleted node reads 'Rewrite the title of “#1”' while Allow reports 'Granted — 0 acts done' — done c758bd4 (a move reverts only if its map exists and the flash says when it did not; asks with gone targets read "✕ … no longer possible", Allow hides when none remain, and reports N of M)
- [ ] **B112** · medium · s · The Forge digest selects node visibility but never sends it, so the mind asks to 'reveal' things blind and takes player markers as DM canon — done c758bd4 (nodes carry vis dm/player + author; placements carry x/y and dm marks; rulebook rules player markers non-canon)
- [ ] **B113** · medium · s · Forge digest keeps the OLDEST 400 nodes and an arbitrary 600 placements and 300 links, so a long campaign's newest things vanish from the mind's view — done c758bd4 (newest first, the current map's things first; links/placements ordered and counted; the Party is one entry; the DM sees a note when the view was trimmed)
- [ ] **B114** · medium · s · A Forge chat turn writes the mind's memory and art style from a copy read when the turn started, erasing any 'Save the mind' made while it ran, and any other turn's recap — done c758bd4 (lore appended in SQL, art_style filled only if empty at write time; one chat turn per world at a time — 409)
- [ ] **B115** · low · xs · The Forge validator throws on a null or bare-string entry in the mind's batch: the turn 500s with a raw TypeError, skips the repair round and is not saved — done c758bd4 (null/bare-string entries become validator errors; server/test/contract.test.js runs locally)
- [ ] **C096** · low · xs · Forge Keep and Refuse answer 200 {ok:true} for a batch that is not in the URL's world (or no longer pending), while Allow and Discard answer 404 — done c758bd4
- [ ] **D023** · low · xs · Forge chat still selects and passes context.mapTitle / context.nodeTitle, which the mind stopped reading in 2c42802 — done c758bd4
- [ ] **O026** · low · xs · The backdrop painter's 'features' prompt uses a filter written for two visibility tiers: players' self-typed markers and every Party footstep feed the paid image prompt — done c758bd4 (Party excluded, one feature per node)

## Items

### B109 · Forge output that touches existing things reaches players as soon as a batch lands, before Keep, although the docs and the batch card say generated things stay DM-only

Broken · high · effort m · second pass, found by `spec`

- **Where:** server/forge/contract.js:314-323 (interior on an existing node), 341-347 (enrich.body), 355-363 (enrich.image and pin flip), 398-406 (links), 413-427 (standing and timed backdrops); served at once by server/routes/share.js:57-66 (walkUp), 198, 260, 303-336; server/forge/mind.js:18 and 96-111 (visibility dropped from the digest); client/src/pages/AtlasWorkspace.jsx:2281 (card copy); CLAUDE.md:145, 150-152 and 155-157.
- **What happens:** Only new nodes and their placements are inserted as 'dm' (contract.js:307-309, 328-330). Everything else writes onto existing rows with no visibility gate, and share.js serves it at once. (1) enrich.body fills an empty body on a shared node (share.js:303, 326). (2) enrich.image attaches art and turns a bare node into an image pin (share.js:169-170, 199, 330). (3) A backdrop with a null start replaces an existing public map's standing art (share.js:147, 260). Timed backdrops enter the windowed payload (share.js:208-217), and map_backdrops has no visibility column. (4) Links between existing shared nodes become players' Threads (share.js:223-226, 317-322). (5) A map owned by an existing shared node passes walkUp (share.js:57-66), so players get ◎ and can enter it and see its painted backdrop (hasInterior at share.js:198). None of this waits for Keep; the 45 s poll picks it up. Unmake later removes it, but players have already seen it. Meanwhile the batch card says '… — new things stay DM-only until you reveal them' (AtlasWorkspace.jsx:2281). The ✦ Paint routes (forge.js:186-221) UPDATE nodes.image_id and maps.image_id directly. That is outside contract.js, which CLAUDE.md:145 calls 'the ONLY write path', and outside any batch, so those paintings have no Keep or Unmake at all. The digest SELECTs visibility and then drops it (mind.js:96-111), and the rulebook says 'never concern yourself with visibility' (mind.js:18), so the mind cannot know that the node it is enriching is public. CLAUDE.md also contradicts itself on reveal. :152 says 'The mind never sets visibility; revealing stays a per-node DM act', and the ask list at :155-157 omits reveal. Yet :180, mind.js:57/64 and contract.js:488-494 all support reveal asks, and one Allow click runs every ask in the batch together.
- **Why it matters:** CLAUDE.md:150-152 and contract.js:5-6: nothing a batch generates reaches players until the DM keeps and reveals it. At the least, the card should say which parts are already public.
- **Fix:** Either apply effects on existing shared things only on Keep, or have share.js hide them while their batch is pending: exclude maps, links and map_backdrops whose ids are in a pending batch's created lists, and serve the recorded prev values for mapBases, enrichedImages and enrichedBodies (add a prev for bodies). At the least, give each existing node's visibility back to the digest so the mind knows what is public, and make the batch card name which parts are already live for players. Reword CLAUDE.md:150-152 and contract.js:5-6 so 'born DM-only' covers only new nodes and placements, and add reveal to the ask list at CLAUDE.md:155-157. Leave the picker's ✦ Paint routes as they are, since they are a documented direct DM act; qualify 'the ONLY write path' at CLAUDE.md:145.
- **Repro:** Code path, not run live because paid AI is forbidden. With the share link open on a public map, send '✦ Forge › paint this map'. The mind returns images plus backdrops [{map: current, start: null}] (mind.js:22). contract.js:416-420 runs UPDATE maps SET image_id. GET /api/share/<token>/maps/<id> then returns the new backdropUrl before Keep. The same holds for 'paint <selected shared node>' (enrich.image) and for build requests that link to or hang interiors on existing public nodes. GET /api/forge/status on production returns {"enabled":true}.
- **Evidence:** Code lines cited above; live /api/forge/status = {"enabled":true}; batch card string at AtlasWorkspace.jsx:2281.
- **Re-proved:** Re-proved by reading the code. contract.js inserts only new nodes (:307) and placements (:328, :394) as 'dm'. The rest writes to existing rows with no gate. (a) :314-323: a map whose owner is an existing node sets that node's interior_map_id. walkUp (share.js:57-66) checks only the owner's visibility and a present placement, so players get ◎ (hasInterior, share.js:198), can enter, and see the new map's title in the breadcrumb and its painted backdrop (share.js:260). That can leak a secret interior the mind named freely. (b) :341-347: enrich.body fills an empty body, served at share.js:303/326…
- **New versus the first pass:** Fully verified against server/forge/contract.js: enrich.body (UPDATE nodes SET body, no visibility gate), enrich.image (UPDATE nodes SET image_id + pin flip, no gate), enrich.stance, links (INSERT INTO links, no visibility column at all on that table), and backdrops (UPDATE maps SET image_id for standing art, INSERT INTO map_backdrops for timed - map_backdrops has no visibility column) all write …
- **Related:** [B003](WP-06-forge-recaps-and-voice.md) — Once enrich facts land, a recap's facts on shared nodes reach players as soon as the batch is written, before Keep, as enrich bodies, images, links and backdrops already do (B109).

### B110 · extends B002: Unmake also deletes what the DM built on top of a creation. That covers paintings the DM reused (as node art, a base backdrop, a timed backdrop or the style anchor) and every hand-made placement, Party footstep and player marker inside a Forge-built interior. Nothing gets a tombstone

Broken · high · effort m · second pass, found by `references`

- **Where:** ✦ Forge › batch card › Unmake; server/forge/contract.js:532-535 (DELETE … WHERE id = ANY for placements…maps…images), R2 sweep after COMMIT; FKs in server/config/schema.sql:83, 121, 251, 270 (image refs SET NULL / CASCADE), 133 (placements.map_id CASCADE), 157 (maps.owner_node_id CASCADE); flash at client/src/pages/AtlasWorkspace.jsx:2268
- **What happens:** Local sandbox (repo HEAD server.js, private Postgres, Gemini and R2 stubbed). Batch 1 painted one image and built 'Forge Tower' with the interior 'Tower — inside'. The painting automatically became the world's style anchor. After that, by hand: I set the painting as a hand-made node's art, as the root map's base backdrop and as a timed backdrop from 60. I revealed the tower, placed an existing hand-made NPC inside it, created 'Hand-made room' inside, and added a Party footstep (50–59) inside. A player also dropped a marker inside through the share API. Unmake returned 200 and the flash read 'Unmade — everything that creation added is gone'. Afterwards: the hand-made node's image_id was null, the root map's image_id was null, the timed backdrop row was gone, world_minds.style_image_id was null and the R2 object was deleted. Every placement inside the tower was gone. 'Hand-made room' and the player's marker 'We hid the key here' are now unplaced nodes, the NPC lost its tower spot, and the Party's footstep 50–59 was destroyed. The unmake wrote 0 tombstones.
- **Why it matters:** Unmake removes only what the batch added. A painting the DM has since used outside the batch stays, or the DM is asked first. Placements of non-batch nodes inside a batch-built map (and any interior the DM gave a batch-born node) are not wiped silently. They are refused with a count, moved out, or tombstoned so Undo can bring them back.
- **Fix:** In discardBatch, before the DELETE loop: (1) images: leave out ids still referenced outside the batch. That means nodes.image_id on nodes not in created.nodes, maps.image_id on maps not in created.maps, map_backdrops rows not in created.backdrops, and world_minds.style_image_id. Otherwise clear them explicitly and report it. (2) maps: count placements on created.maps whose id is not in created.placements, plus maps whose owner_node_id is in created.nodes but that are not in created.maps. If any exist, return them so the client's confirm (B002) can say '3 things you placed inside Forge Tower would lose their spot'. Then either re-home them or snapshot them into a tombstone.
- **Repro:** lanes/references/forge-sb.mjs against the local sandbox (up.sh with GEMINI_API_KEY=stub STUB_R2=1 STUB_QUEUE): queue a batch with images:[i1], nodes:[tower image=i1 placed on root], maps:[{owner:'tower'}]. Chat, then PATCH /nodes/<hand-made> {image_id}, PATCH /maps/<root> {image_id}, POST /maps/<root>/backdrops {image_id,start_time:60}, reveal the tower, POST /maps/<tower-inside>/placements for an existing node and the Party, POST /maps/<tower-inside>/nodes, and POST /api/share/<tok>/maps/<tower-inside>/nodes. Finally POST /api/forge/worlds/:id/batches/:id/discard.
- **Evidence:** forge-sb.json: 'before Unmake {X:{image_id:1}, root:{image_id:1}, timed:1, anchor:1, placementsInTower:[Hand-made NPC, Hand-made room, The Party, We hid the key here]}' then 'after Unmake {X:{image_id:null}, root:{image_id:null}, timed:0, anchor:null, imageRow:0, nodesNowUnplaced:[Hand-made room (shared), We hid the key here (player)], NPCplacements:[{map_id:1}], partySteps:[{map_id:1,40,49}] (the 50–59 step inside is gone), tombstonesAdded:0}' and 'r2 deletes [worlds/1/forge-…png]'.
- **Re-proved:** I re-proved this on my own local harness: lanes/verify-references-r0/harness.mjs plus forge-reprove.mjs. It runs PGlite (real Postgres) on the repo's schema.sql, with the UNMODIFIED worlds/atlas/share/forge routes, contract.js and mind.js. Only db, auth, R2 and Gemini are stubbed. Batch 1 painted image 1 and built 'Forge Tower' with interior map 2. Through the real routes I then set image 1 as a hand-made node's art (PATCH 200), as the root base backdrop (200) and as a timed backdrop (201). I placed a hand-made NPC in the tower, created a room there, added a Party step 50–59 and dropped a sha…
- **New versus the first pass:** Read discardBatch in server/forge/contract.js:522-586 directly. It does `DELETE FROM images WHERE id = ANY($1)` for every image the batch created, and `DELETE FROM maps WHERE id = ANY($1)` for every map it created, both unconditionally -- with no check for references outside the batch's own tracked fields (mapBases, enrichedImages, noteAppends, style anchor) and no check for placements of non-bat…
- **Extends:** [B002](WP-06-forge-recaps-and-voice.md) — Unmake also deletes images the DM reused elsewhere (node art, backdrops, the style anchor) and every placement inside a Forge-built map, including hand-made nodes, Party footsteps and player markers. The confirm must count these, and discardBatch must skip or re-home them.

### B111 · Forge asks outlive their targets: an allowed move whose origin map is later removed makes Unmake fail forever with a raw foreign-key error, and a pending ask on a deleted node reads 'Rewrite the title of “#1”' while Allow reports 'Granted — 0 acts done'

Broken · medium · effort s · second pass, found by `references`

- **Where:** ✦ Forge › batch card. server/forge/contract.js:557-561 (the move revert writes the old map_id back unchecked, inside the one transaction); server/routes/forge.js:26-27 (wrap returns err.message), 58-60 (askLine falls back to `#${id}`); client/src/pages/AtlasWorkspace.jsx:2257 ('Granted — N acts done'), 2292 ('✓ permission granted — Unmake reverts it all')
- **What happens:** Sandbox, same server code as production. Batch 2 asked to move 'Hut dweller' from the Hut's interior to the root, and Allow returned {granted:1}. The DM then removed the Hut's interior (inspector ✕). Unmake returned 500 {"message":"insert or update on table \"placements\" violates foreign key constraint \"placements_map_id_fkey\""}, and a second Unmake gave the same result. The batch stays status 'pending', asks_state 'allowed'. Its card keeps saying 'Unmake reverts it all', but nothing it created or changed can be unmade any more, and every attempt rolls back. Batch 3 asked to rename node 1, and the DM then deleted node 1. The card read 'Rewrite the title of “#1”', and Allow returned {granted:0, requested:1}, which the client flashes as 'Granted — 0 acts done'.
- **Why it matters:** Unmake reverts what it still can and says what it could not: a move whose origin map is gone stays where it is, or goes to the owner's map. An ask whose target no longer exists reads 'no longer possible' and cannot be allowed. Allow names anything it skipped.
- **Fix:** In discardBatch's move revert, use `UPDATE placements SET map_id=$1,x=$2,y=$3 WHERE id=$4 AND EXISTS (SELECT 1 FROM maps WHERE id=$1)`, collect the reverts it skipped and return them in the response for the flash. In GET /forge/worlds/:id askLine, mark asks whose node, map or era is missing ('✕ no longer exists') and omit Allow when every ask on the card is gone. Have allowAsks return the skipped asks so the client flash says what did not happen.
- **Repro:** lanes/references/forge-sb.mjs scenarios C and D: queue {batch:{asks:[{op:'move',node:W,map:I,to_map:root}]}}, chat, POST …/batches/:id/allow, DELETE /api/atlas/nodes/<owner of I>/interior, then POST …/batches/:id/discard twice. Queue an edit ask on node X, DELETE /api/atlas/nodes/X, GET /api/forge/worlds/:id, then POST …/allow.
- **Evidence:** forge-sb.json: 'Allow C {granted:1}', 'DM removes the hut interior 200', 'Unmake C (1st) 500 … placements_map_id_fkey', 'Unmake C (2nd) 500 …', 'C status {status:pending, asks_state:allowed}', 'card text for D [["Rewrite the title of “#1”"]]', 'Allow D {granted:0, requested:1}'. server.log shows the FK violation stack.
- **Re-proved:** Re-proved locally with the unmodified forge.js, contract.js and atlas.js on PGlite (lanes/verify-references-r0/forge-reprove.mjs, output forge-reprove.json). A move ask took Dweller from the Hut interior (map 2) to root. Allow returned {granted:1}, and the placement now sat on map 1. DELETE /nodes/hut/interior returned 200. Unmake #1 and #2 both returned 500 {message:'insert or update on table "placements" violates foreign key constraint "placements_map_id_fkey"'}. The batch stays status 'pending', asks_state 'allowed', and GET /forge still lists it. Cause: contract.js:560 writes u.map_id bac…
- **New versus the first pass:** Read allowAsks and discardBatch in server/forge/contract.js directly. allowAsks (457-end) re-checks each ask's targets against the world and silently `continue`s past ones that no longer exist, returning `{granted: undo.length, requested: (b.asks||[]).length}` -- confirming the 'Granted -- 0 acts done' scenario when every target is gone. discardBatch's move-revert (`UPDATE placements SET map_id=$…
- **Related:** [B002](WP-06-forge-recaps-and-voice.md) — If an allowed move's origin map was later removed, every Unmake fails with a foreign-key error. The compare-and-set restores should also skip targets that no longer exist and report them.

### B112 · The Forge digest selects node visibility but never sends it, so the mind asks to 'reveal' things blind and takes player markers as DM canon

Broken · medium · effort s · second pass, found by `copiers`

- **Where:** server/forge/mind.js:96-99 (visibility selected) vs :111 (dropped), :101/:104 (links/placements LIMIT with no ORDER BY or note), :102-104 (placements lack visibility, x/y), :18 (stale 'never concern yourself with visibility') vs :24/:64 (recap and reveal asks), :162; server/forge/contract.js:488-490 (non-dm reveal skipped silently), :513 (granted=undo.length); server/routes/forge.js:68 (card text ignores current visibility); client/src/pages/AtlasWorkspace.jsx:2257
- **What happens:** I ran the real digest() locally against a stub DB with three nodes: a DM-only villain, an innkeeper players already met (shared), and a player marker ('we hid the gold here'). The mind receives [{id:101,…,secret:'Poisoner.'},{id:102,title:'Already-met innkeeper',…},{id:103,title:'A player marker',body:'we hid the gold here'}], with no field telling hidden, revealed and player-made apart. Placements arrive as {n,m,s,e} only: no visibility (Forge-born placements are DM-only) and no position. The only node whose visibility the mind learns is the FOCUS node. Reveal asks for already-shared nodes are dropped at Allow without a word ('Granted — 2 acts done' for a card that listed 3). A player's 500-character marker body reaches the mind as world content under an instruction to trust the digest over the chat.
- **Why it matters:** The digest carries what the rulebook asks the mind to reason about. Each node's visibility is 'dm', 'shared' or 'player', with player markers labelled as player notes. Placement visibility and position are included. Truncation is deterministic and announced, the way the node list already announces it.
- **Fix:** In digest(), emit a per-node visibility field (e.g. vis:'dm'|'shared'|'player'), or omit it for shared nodes and mark 'dm' and 'player'. Label player markers, e.g. {player:true, author}, or leave them out of 'canon'. Add vis and x/y to placements. Give the links and placements queries an ORDER BY and add a truncation note like the nodes note. Rewrite RULEBOOK line 18 so it no longer says 'never concern yourself with visibility', and state that reveal applies only to vis:'dm' nodes. Optionally have allowAsks report skipped asks (it already returns requested) and show 'N of M' in the Allow flash.
- **Repro:** NODE_PATH=server/node_modules node lanes/copiers/digest.cjs (local only; runs server/forge/mind.js digest() against a stub pool).
- **Evidence:** digest.out: node objects lack visibility even though the executed SQL is `SELECT id, title, category, visibility, … FROM nodes … LIMIT 400`; placements are `[{n:101,m:10,s:null,e:null},…]`; the links and placements SQL have LIMIT and no ORDER BY. git: `visibility` has been selected and dropped since d8d5a38 (2026-08-20, when the rulebook said 'never concern yourself with visibility'). Reveal asks arrived in 2c42802 (2026-09-24) without adding it to the digest.
- **Re-proved:** Re-proved from the code and a local run of the real digest() against a stub pool. mind.js:96-99 selects `visibility`, but the node object at :111 emits only id/title/cat/body/secret/interiorMap. Placements (:102-104) are {n,m,s,e} with no visibility or x/y. The links (:101, LIMIT 300) and placements (:104, LIMIT 600) queries have no ORDER BY and no truncation note, unlike the nodes note at :112. The only node whose visibility reaches the mind is the FOCUS node (:153). The RULEBOOK still says 'never concern yourself with visibility' (:18, from d8d5a38), yet the recap (:24) and the reveal ask (…
- **New versus the first pass:** Verified in mind.js: digest() selects n.visibility (96-99) but the emitted node object (111) omits it entirely; placements are emitted as {n,m,s,e} only (102-104), no visibility or x/y; links (LIMIT 300) and placements (LIMIT 600) have no ORDER BY and no truncation note, unlike nodes which get one at :112. Re-ran the finding's own digest.cjs locally against a stub pool: output matches the finding…
- **Related:** [O016](WP-06-forge-recaps-and-voice.md) — When rewriting the rulebook, also drop line 18's 'never concern yourself with visibility' and send each node's visibility in the digest. Today the mind asks to reveal things without knowing what players already see.

### B113 · Forge digest keeps the OLDEST 400 nodes and an arbitrary 600 placements and 300 links, so a long campaign's newest things vanish from the mind's view

Broken · medium · effort s · second pass, found by `big-world`

- **Where:** server/forge/mind.js:96-99 (nodes ORDER BY id LIMIT 400: the oldest 400 kept), :101 (links LIMIT 300, no ORDER BY), :102-104 (placements LIMIT 600, no ORDER BY, no is_active filter), :112 (the truncation note covers nodes only; placement and link truncation is silent); the context block at :140-143 sends the current map's title and notes, never its contents.
- **What happens:** Code-proven (paid calls are off limits). The digest sent every turn lists nodes ORDER BY id LIMIT 400. Past 400 nodes the dropped ones are the NEWEST: the places and people from recent sessions that a recap refers to. The mind is told only '<N> more nodes not shown'. The rulebook still says 'check the digest first and never rebuild what already exists' (mind.js:77), so it will re-create them. Placements and links are capped with no ORDER BY, so which rows the mind sees is up to the query plan. The Party alone has one placement per footstep: 200 in my world, 400 at 40 sessions under the table convention. A 40-session world with about 300 ordinary placements already passes 600, and random nodes look unplaced to the mind. My live world (352 nodes, about 530 placements) stayed just under both caps. The Forge makes up to 60 nodes per batch, so 400 is about seven builds.
- **Why it matters:** When the digest has to truncate, it keeps what matters (recent, on the current map, alive at canon) in a stable order. The Party's footsteps collapse to its live step plus a count.
- **Fix:** Order nodes by recency or relevance (e.g. updated_at DESC, with nodes on the context map and the focus node's neighbours first) before LIMIT. Add ORDER BY to placements and links, prioritising the context map. Emit one entry for category 'party' (its live placement plus 'N footsteps') instead of every footstep. Tell the DM in the panel when the digest was truncated.
- **Repro:** Code: mind.js:96-104. A world with 401+ nodes: the node with the highest id is absent from digest(worldId).nodes. With 601+ placements, the returned set depends on the plan (no ORDER BY).
- **Evidence:** mind.js:99 `FROM nodes WHERE world_id=$1 ORDER BY id LIMIT 400`; :101 `SELECT … FROM links WHERE world_id=$1 LIMIT 300`; :104 `… FROM placements p JOIN maps mp … WHERE mp.world_id=$1 LIMIT 600`. Live world counts from lanes/big-world/build.json (342 created + 10 sample nodes; 200 party footsteps).
- **Re-proved:** Code-proven. server/forge/mind.js:96-99 selects nodes `FROM nodes WHERE world_id=$1 ORDER BY id LIMIT 400`. nodes.id is SERIAL (schema.sql:115), so past 400 nodes the digest keeps the 400 OLDEST and drops the newest. :101 caps links at `LIMIT 300` with no ORDER BY. :102-104 caps placements at `LIMIT 600` with no ORDER BY, and does not filter mp.is_active either, so rows on inactive maps use up the cap. Only nodes get a truncation note (:112 `${nodeCount - 400} more nodes not shown`). Placement and link truncation is silent, so a node whose placements fall past row 600 looks unplaced to the mi…
- **New versus the first pass:** New: not the same root cause as O016 (WP-06, vocabulary mismatch: rulebook says 'years'/'six categories', digest includes a 'party' category — a prompt-wording issue) or C004 (WP-06, character-count trims on the bible/lore text fields at 60k/6k/20k). This finding is about row-count LIMIT truncation with no ORDER BY on structured digest data (nodes/links/placements), a different mechanism and cons…
- **Related:** [O016](WP-06-forge-recaps-and-voice.md) — The digest sends every Party footstep as a placement and keeps the oldest 400 nodes. Send the Party as one entry and order the caps by recency.

### B114 · A Forge chat turn writes the mind's memory and art style from a copy read when the turn started, erasing any 'Save the mind' made while it ran, and any other turn's recap

Broken · medium · effort s · second pass, found by `lost-updates`

- **Where:** server/forge/mind.js:123 (mind = ensureMind() at turn start), :199-202 (lore = `${mind.lore}\n${lore_append}`), :203-205 (art_style written when the start-of-turn copy was empty); server/routes/forge.js:92-128 (PATCH /mind). Trigger: ✦ Forge › Send a build or paint (each painting takes a minute), then ⚙ › edit Memory or Art style › Save the mind. The button stays enabled during a turn (AtlasWorkspace.jsx:2355, disabled only when busy==='mind').
- **What happens:** A local harness mounted the repo's own routes/forge.js and forge/mind.js on Express, with an in-memory pool, a stub gemini and stub auth. (1) PATCH /mind sent 300 ms into a 1.5 s turn returned 200 and stored the DM's memory line and 'soft ink and watercolour, muted blues'. When the turn ended, world_minds held the start-of-turn lore plus the turn's append, and art_style was the mind's drafted 'grim woodcut, sepia wash'. The DM's line ('Brakk is secretly Marra's brother — never reveal before session 6') and the DM's art style were gone. (2) With two overlapping turns (two tabs, or the second message B063 lets through), only the slower turn's lore_append survived. The UI flashes 'The mind took it in' for the lost save.
- **Why it matters:** A turn appends to the lore as it is when the turn ends, and drafts an art style only if it is still empty then. A DM edit made mid-turn survives.
- **Fix:** Do the append in SQL: UPDATE world_minds SET lore = RIGHT(BTRIM(COALESCE(lore,'') || E'\n' || $1, E'\n'), 20000). For art style: art_style = CASE WHEN COALESCE(art_style,'')='' THEN $2 ELSE art_style END. Optionally refuse a second chat per world while one runs (409), which also closes B063 on the server side.
- **Repro:** node /tmp/claude-1000/-home-bennett-repos/8ae59c6f-6998-4f5b-912f-a3516576c464/scratchpad/tl/lanes/lost-updates/hmind.cjs (local only: no network, no database, the repo is only required)
- **Evidence:** hmind.cjs output: '1. PATCH /mind during the turn -> 200 | mind right after the PATCH: {"lore":"…\nDM: Brakk is secretly Marra's brother…","art_style":"soft ink and watercolour, muted blues"}' then 'world_minds after the turn ends: {"lore":"Session 1 …\nSession 2 (2026-09-26): Marra told them the gate code.","art_style":"grim woodcut, sepia wash"}'. '2. … lore after both turns: "Session 1 …\nSession 2 (2026-09-26): the party burned the docks."' (the harbourmaster thread is lost).
- **Re-proved:** Re-proved by code and a local harness. converse() (server/forge/mind.js:122-123) reads `mind` once at turn start. After one or two generateJSON calls and applyBatch (which can include paintings), it writes lore = `${mind.lore}\n${lore_append}` from that start-of-turn copy (:199-202). It writes art_style whenever the start-of-turn copy was empty (:203-205), with no re-read and no CASE guard. PATCH /worlds/:id/mind (routes/forge.js:92-128) is a plain UPDATE. The chat route (:133-149) has no per-world lock (grepping routes/forge.js and server/forge/ for lock or inflight finds none). The client's…
- **New versus the first pass:** Verified against server/forge/mind.js: converse() reads `const mind = await ensureMind(worldId)` once at the top (matches cited :123), and at the end builds the lore/art_style UPDATE from that same stale in-memory `mind.lore`/`mind.art_style` (matches cited :199-205), never re-reading the row before writing. This is a distinct server-side lost-update, different in mechanism, file and fix from B00…
- **Related:** [B001](WP-06-forge-recaps-and-voice.md) — Patching only the edited fields fixes the panel, but the server's chat turn still writes lore and art style from a copy read when the turn started, erasing any Save made during the turn. Do the append in SQL.
- **Related:** [B063](WP-06-forge-recaps-and-voice.md) — A separate savingMind state keeps 'Save the mind' usable during a turn, and that save is erased when the turn ends (B114). Prefer the `if (busy) return` guard until the server-side append is fixed.

### B115 · The Forge validator throws on a null or bare-string entry in the mind's batch: the turn 500s with a raw TypeError, skips the repair round and is not saved

Broken · low · effort xs · second pass, found by `crash`

- **Where:** server/forge/contract.js:45-183 (validateBatch loops over images :45, nodes :54, placements :68, facts :76, enrich :85/:93/:100, enrich_maps :110, asks :121, maps :144, links :172, eras :177, backdrops :183), called outside any try at server/forge/mind.js:169 and :175; ✦ Forge › chat
- **What happens:** The repo's validateBatch, called locally, throws a TypeError for 13 shapes, among them nodes: ["The Old Mill"] → 'n.placements is not iterable', nodes: [{…, placements:[null]}] → "Cannot read properties of null (reading 'x')", and asks/links/eras/maps/backdrops/enrich/enrich_maps/images: [null]. Driving the real /api/forge router with a stubbed mind (forge-turn.cjs): a reply with such a batch gives HTTP 500 {"message":"n.placements is not iterable"}. The only DB write is the world_minds ensure, so the DM's message, the mind's reply and its lore_append are all dropped, and no repair round is attempted. The client flashes the raw TypeError (AtlasWorkspace.jsx:2246) and the DM's typed message is gone after a reload. Control: nodes: "x" (wrong type, not an array) is caught by the validator's own check. The repair round runs, and the turn is saved with HTTP 200.
- **Why it matters:** Malformed entries become validator errors. They go through the repair round like every other structural problem, and a turn that still fails is saved with applyError, as the 'proposal stayed malformed' path at mind.js:184-186 does.
- **Fix:** At the top of validateBatch, drop or flag non-object entries: for every top-level list and every inner list (placements, facts, place), keep only entries that are plain objects, and push an error such as 'nodes[3] must be an object' for each one dropped. Alternatively wrap the validateBatch calls in mind.js:169/:175 so a throw becomes errs = [e.message] and flows into the repair round.
- **Repro:** cd /tmp/claude-1000/-home-bennett-repos/8ae59c6f-6998-4f5b-912f-a3516576c464/scratchpad/tl/lanes/crash && node validator.cjs; node forge-turn.cjs (stub pool and stub Gemini; no paid call).
- **Evidence:** validator.out: 13 THROWS lines, 3 OK controls. forge-turn.out: case A HTTP 500, 'repair rounds used: 0', DB writes only 'INSERT INTO world_minds'. Case C (control) HTTP 200 with the mind_messages INSERTs. contract.js normalizes only the top-level arrays (:28-31) and inner arrays (Array.isArray), never the elements, and the file is sloppy-mode CommonJS, so a bare-string node fails only at the inner for…of.
- **Re-proved:** Re-proved locally. verify-crash-r0/val.cjs requires the real contract.js. validateBatch throws a TypeError for nodes:['The Old Mill'] ('n.placements is not iterable': the file is sloppy CommonJS with no 'use strict', so writing a property to a primitive fails silently), and for nodes/images/links/asks/maps/enrich:[null], a null placement and a null fact. Controls: nodes:'x' → ['nodes must be an array'], and eras:['x'] does not throw. Running forge-turn.cjs from my lane (the real /api/forge router, stub pool, stub Gemini) gives case A HTTP 500 {"message":"n.placements is not iterable"} with 0 …
- **New versus the first pass:** Verified: validateBatch throws unguarded TypeErrors on null/bare-string batch entries (13 reproduced shapes), called with no try/catch in mind.js before the repair round or message inserts. The first pass's own docs/cleanup/appendix.md:139 explicitly admits validator-clamp internals were 'not covered ... only the write statements were read,' confirming this is new ground rather than a duplicate o…
- **Related:** [B003](WP-06-forge-recaps-and-voice.md) — validateBatch throws before applyBatch on a null or bare-string entry, so the whole turn returns 500 and is not saved. Add those shapes to the contract probe you keep as a regression check.

### C096 · Forge Keep and Refuse answer 200 {ok:true} for a batch that is not in the URL's world (or no longer pending), while Allow and Discard answer 404

Confusing · low · effort xs · second pass, found by `cross-tenant`

- **Where:** server/routes/forge.js:153-162 (POST /worlds/:worldId/batches/:id/keep) and :172-178 (/refuse), vs :166-171 (/allow) and :179-184 (/discard); client handlers client/src/pages/AtlasWorkspace.jsx:2251-2273 (askAct/batchAct). Atlas › ✦ Forge › batch card Keep / Refuse
- **What happens:** Live, as a fresh guest (user 31) owning world 167, with batch id 1 (not in world 167): POST /api/forge/worlds/167/batches/1/keep → 200 {"ok":true}; /refuse → 200 {"ok":true}; /allow → 404 'No pending asks on that batch'; /discard → 404 'No such pending batch'. The UPDATE's `WHERE id=$1 AND world_id=$2 AND status='pending'` matches nothing, so nothing leaks or changes, but the route reports success. The same happens when the card is stale, for example when the batch was already unmade in another tab: Keep still 'succeeds'.
- **Why it matters:** The same status for the same failure: 404 'No such pending batch' when the UPDATE matched no row, as allow and discard already answer.
- **Fix:** Add `RETURNING id` to both UPDATEs (or check rowCount). When no row matched, keep should answer 404 {message:'No such pending batch'}, the same as discard, and refuse should answer 404 {message:'No pending asks on that batch'}, the same as allow. The client already shows errText on failure. Optionally, batchAct/askAct could refresh the batch list on a 404 so a stale card corrects itself instead of just showing an error.
- **Repro:** As any account with one world W and no Forge batch 1 in it: POST /api/forge/worlds/W/batches/1/keep, then /refuse, /allow, /discard. Compare the statuses. Requests with a foreign world in the URL correctly 404 on ownsWorld first.
- **Evidence:** lanes/cross-tenant/probe.out: '!! POST batch keep (B world, batch not B's) -> 200 {"ok":true}', '!! POST batch refuse (B world, batch not B's) -> 200 {"ok":true}', 'POST batch allow ... -> 404', 'POST batch discard ... -> 404'.
- **Re-proved:** Re-proved from the code and in a local sandbox, with no writes to production. In server/routes/forge.js, the keep handler (153-162) and the refuse handler (172-178) run an UPDATE ... WHERE id=$1 AND world_id=$2 AND status='pending' (or asks_state='pending'). Neither checks rowCount or uses RETURNING, and both always reply res.json({ok:true}). Allow (166-171) and discard (179-184) go through contract.js allowAsks (457-461) and discardBatch (522-526), which SELECT first and return null, so those two answer 404. I mounted the real forge.js router locally against a stub pool (lanes/verify-cross-t…
- **New versus the first pass:** Verified new against both overlap candidates it names. B023 (docs/cleanup WP-01) is a distinct defect in the same file: cross-world name resolution in the GET panel's nameOf helper, nothing to do with keep/refuse status codes. C069 (docs/cleanup WP-20) catalogs inconsistent wrap()/ownsWorld() status codes for the 'not yours' failure across different route files (atlas.js, share.js, forge.js, voic…
- **Extends:** [C069](WP-20-structural-cleanup-split-the-workspace-share-hel.md) — Forge Keep and Refuse answer 200 {ok:true} when no batch matched, while Allow and Discard answer 404. Check rowCount in those two routes when you unify the status codes.

### D023 · Forge chat still selects and passes context.mapTitle / context.nodeTitle, which the mind stopped reading in 2c42802

Dead code · low · effort xs · second pass, found by `history`

- **Where:** server/routes/forge.js:140-141, 145-146; server/forge/mind.js:137-146
- **What happens:** Before 2c42802, converse() built its 'where' line from context.mapTitle and context.nodeTitle. 2c42802 ('no modes') replaced that with a full re-query by id (mind.js:141 and :146 select the map and node again with notes, stance, facts and threads). forge.js:140/:145 still SELECT id, title and store context.mapTitle/nodeTitle, and `grep -n context server/forge/mind.js` shows only context.mapId and context.nodeId are read now. Each chat turn therefore runs two ownership queries that fetch unused titles, followed by two more queries for the same rows.
- **Why it matters:** The route verifies ownership with SELECT id, and passes ids only (or the verified rows, so mind.js does not re-query).
- **Fix:** In server/routes/forge.js:140 and :145, change the queries to `SELECT id FROM maps|nodes WHERE id=$1 AND world_id=$2` and delete the `context.mapTitle = m.title` and `context.nodeTitle = n.title` assignments. Keep these world-scoped SELECTs: they are the only ownership check, because mind.js:141 and :146 look rows up by id alone. Optional: do the full-detail SELECTs in the route with world_id=$2 and pass the rows into converse(), then delete mind.js's second lookups. That saves two queries per chat turn.
- **Repro:** git show 2c42802 -- server/forge/mind.js | grep -n 'mapTitle\|nodeTitle' (lines removed there); grep -n 'mapTitle\|nodeTitle' server → only forge.js:141,146.
- **Evidence:** 2c42802 diff: `- const where = context?.mapTitle ? … context.mapTitle … context.nodeTitle …`; HEAD forge.js:141 `context.mapTitle = m.title`, :146 `context.nodeTitle = n.title`; mind.js reads only context?.mapId (:140) and context?.nodeId (:144).
- **Re-proved:** I re-proved this against HEAD 6dee15d. At server/routes/forge.js:140 and :145 the route runs `SELECT id, title FROM maps|nodes WHERE id=$1 AND world_id=$2`, and :141 and :146 set context.mapTitle and context.nodeTitle. `context` goes only to converse() (forge.js:148). In server/forge/mind.js, grep -n context finds context used only at lines 121-146, and only context?.mapId (:140, :141) and context?.nodeId (:144, :146) are read. mind.js then selects the same map (:141) and node (:146) again by id, with dm_note, stance and the rest. A whole-repo grep for mapTitle|nodeTitle (node_modules, dist a…
- **New versus the first pass:** Verified new: no docs/cleanup item covers the mapTitle/nodeTitle dead-field pattern. WP-20 C068/C069 cite forge.js:138/143 only for the camelCase {mapId,nodeId} body-shape naming complaint, not these fields; WP-07 D019 is an unrelated file. Code verified: git log -S mapTitle on mind.js/forge.js shows only commit 2c42802, which removed mind.js's use of context.mapTitle/nodeTitle in favor of re-que…

### O026 · The backdrop painter's 'features' prompt uses a filter written for two visibility tiers: players' self-typed markers and every Party footstep feed the paid image prompt

Obsolete · low · effort xs · second pass, found by `history`

- **Where:** server/routes/forge.js:210-212 (feature query), used at :215
- **What happens:** bb4f7a4 (2026-08-25) added `WHERE p.map_id=$1 AND p.visibility != 'dm' AND n.visibility != 'dm' ORDER BY p.id LIMIT 12` with the comment 'hidden things must not steer it'. That filter was written for shared/dm only. It passes visibility='player' nodes (74416a4: anonymous share-link holders type their titles) into the terrain prompt ('with ground for these features (do not label them): …'). It also passes category 'party' (added 09-25). Party footsteps are separate placements and there is no DISTINCT, so 'The Party' can take several of the 12 slots and push out real places on a well-walked map.
- **Why it matters:** Only the DM's own places steer a paid painting: exclude visibility 'player' and category 'party', and use DISTINCT titles.
- **Fix:** forge.js:210-212 → `SELECT n.title FROM placements p JOIN nodes n ON n.id=p.node_id WHERE p.map_id=$1 AND p.visibility != 'dm' AND n.visibility != 'dm' AND n.category <> 'party' GROUP BY n.id, n.title ORDER BY MIN(p.id) LIMIT 12`. This excludes the Party's footsteps and repeated placements of the same node while keeping the oldest-first order. Do NOT use `SELECT DISTINCT n.title … ORDER BY p.id`, which Postgres rejects. Leave visibility 'player' as it is unless Bennett decides markers should not steer paintings.
- **Repro:** Code reading of forge.js:210-216 (a paid call was not made).
- **Evidence:** forge.js:212 filter quoted above; share.js:291 inserts markers with visibility 'player' and a player-typed title; categories.js party added in 66e77f1.
- **Re-proved:** Kept only the Party half. The player-marker half is wrong. git log shows player markers (74416a4, 2026-08-20) existed five days before bb4f7a4 (2026-08-25) wrote `p.visibility != 'dm' AND n.visibility != 'dm'`. So the filter was not 'written for two tiers'. Its stated goal is secrecy ('hidden things must not steer it'), and player markers are public. The Forge mind's digest (server/forge/mind.js:96-99) also feeds every node, markers included, to the AI, so leaving them in is consistent. Whether markers should steer the painting is a design call, not a defect. The Party half holds. Category 'p…
- **New versus the first pass:** Verified new and accurate. Read forge.js:209-216 directly: the query is exactly `SELECT n.title FROM placements p JOIN nodes n ON n.id=p.node_id WHERE p.map_id=$1 AND p.visibility != 'dm' AND n.visibility != 'dm' ORDER BY p.id LIMIT 12`, with no category or DISTINCT filter, feeding the paid backdrop paint prompt. Confirmed player markers are inserted with visibility='player' (share.js's marker ro…
