# WP-20 · Structural cleanup: split the workspace, share helpers, retire legacy columns

Part of the [Atlas cleanup list](README.md) (2026-09-26).

**Goal:** Split the 2,400-line AtlasWorkspace.jsx, and share time resolution, pin markup and modals between the workspace and the Player View. Remove the legacy columns and soft-delete filters from the live schema.

**Do after:** [WP-02](WP-02-protect-the-live-campaign-data-undo-clone-and-ba.md), [WP-03](WP-03-autosave-that-tells-the-truth.md), [WP-07](WP-07-delete-the-dead-code.md), [WP-08](WP-08-docs-config-and-comments-tell-the-truth.md), [WP-10](WP-10-sessions-clock-labels-and-the-party.md), [WP-13](WP-13-the-map-surface-canvas-tree-and-outlines.md), [WP-18](WP-18-keyboard-dialogs-and-screen-readers.md)

**Notes:** Done as one refactor per commit, each deployed and run through every suite before the next. confusing-code-08 already contains a split plan. postures-share-02 is best fixed by rendering the Player posture with the Player View's own code (confusing-code-14) instead of patching a second copy. The column work (server-dead-11, schema-data-11, maps-29) touches the live campaign database. Take a backup, remove the code that reads each column, deploy, and only then drop the column. Re-run every suite after each step.

## Checklist

- [x] **B060** · medium · l · The Player preview is not what the share link shows: it misses the lantern trail, shows extra footprints, the future party step, lifespans and descriptions, and enters DM-only interiors — done ed3e125 (the Player posture is the framed Player View itself)
- [x] **C021** · medium · s · One class name, several meanings (legend, open, tgear, factions, fname, muted, env-status); two caused real bugs — done 14816c1
- [x] **C038** · low · s · Misleading names and hidden logic: nodeLinks holds facts, searchIndex doubles as a visibility index, dropNode means two things, selection parsed from a className — done cbafaa9 (names and the ambience input; the two JSX IIFEs and the short CSS class names stay)
- [x] **C039** · low · m · Workspace and Player View copy-paste pin markup, pointer→plane math and outline constants; STYLE_KEYS is imported but a third hardcoded list is used — done bd8baf0
- [x] **C040** · low · s · Time vocabulary collides: `now` is the DM lens in the workspace but canon in EraScrub; `current` means canon; the effective moment has three names — done cbafaa9 (lens/shownAt; tl.current stays and CLAUDE.md names it canon)
- [x] **C042** · low · s · UI preferences and 'last world' state are hand-rolled many times with inconsistent encodings, guards and magic numbers — done 8fa7c6c
- [x] **C044** · low · s · Time resolution (presence and 'latest-starting row covering t') is implemented about 12 times across client and server — done 08dd708
- [x] **C050** · low · s · z-index is a set of magic numbers with no scale; `.apop` z-index:30 does nothing inside the Map ▾ and ? popovers — done 0de9116
- [x] **C068** · low · m · API shapes differ per route: camelCase body on spotlight/forge context, mixed-case upload body, three shapes for the world timeline, many response envelopes — done 3a3bb9b (world CRUD stays split across worlds.js and the atlas PATCH — both already use the one name rule)
- [x] **C069** · low · m · Server error handling and ownership checks are copy-pasted with different behaviour (wrap x4, ownsWorld x3+, 404/400/403 for the same failure, 403 for expired tokens) — done 8bbb2ce
- [x] **C070** · low · m · Image pipeline naming: the only upload route is called 'base64' and writes to R2; base64 storage is 'legacy' in code but 'deliberate fallback' in the README — done 51f73c8
- [ ] **C083** · low · l · A newcomer can't tell which stylesheet styles what; atlas.scss is a 775-line changelog and the per-page imports imply isolation that doesn't exist — left: reordering atlas.scss by component changes cascade order and needs a visual pass
- [x] **C084** · low · l · AtlasWorkspace.jsx is a 2,408-line / 135 KB file; concrete split plan (what moves where, and in what order) — done 05a8088 (stage one — the seven panels moved out; the timebar, reader and dialogs still live in the workspace)
- [ ] **C090** · low · m · Same job, many looks: 8 'on' treatments for toggles, `.tool.on` doubling as the primary button, 8 close-button classes, 5 section-label styles — left: unifying the control looks is a design change for Bennett to look at
- [x] **D009** · low · s · Legacy maps columns (description, parent_map_id, zoom_level, map_order, is_active) are only copied around, and 'parentMapId' means something else in the API — done 349ce20
- [x] **D010** · low · s · Duplicated helpers: an identical Modal component, plural(), usesOf(), and the 'latest start covering t' resolver written three times — done 08dd708
- [x] **D012** · low · m · Legacy columns nothing reads or writes meaningfully: worlds.settings, maps.description/parent_map_id/zoom_level/map_order, links.kind/time_context, images.tags, folder color/icon — done 349ce20 (the columns were dropped in production on deploy; every value was a default)
- [x] **O023** · low · s · worlds/maps.is_active is a soft-delete leftover: never set to false, yet filtered 29 times; the DELETE route is still labelled '(soft delete)' — done 349ce20 (the one soft-deleted map (id 17, empty) was hard-deleted first)
- [x] **O025** · low · s · 'imageServiceBase64' is really the whole image client, one of its comments points at a file that was deleted, and its upload progress numbers are made up — done 51f73c8

## Items

### B060 · The Player preview is not what the share link shows: it misses the lantern trail, shows extra footprints, the future party step, lifespans and descriptions, and enters DM-only interiors

Broken · medium · effort l · found by `postures-share` (+3 other lanes)

- **Where:** Workspace › 🎭 Player posture (AtlasWorkspace.jsx); tooltip: 'Player shows exactly what the share link shows'
- **Files:** `client/src/pages/AtlasWorkspace.jsx:759-764`, `client/src/pages/AtlasWorkspace.jsx:861-873`, `client/src/pages/AtlasWorkspace.jsx:997-999`, `client/src/pages/AtlasWorkspace.jsx:1051`, `client/src/pages/AtlasWorkspace.jsx:1058`, `client/src/pages/AtlasWorkspace.jsx:1063`, `client/src/pages/AtlasWorkspace.jsx:1101`, `client/src/pages/AtlasWorkspace.jsx:1351-1366`, `client/src/pages/AtlasWorkspace.jsx:1380-1402`, `client/src/pages/AtlasWorkspace.jsx:293-302`, `client/src/pages/AtlasWorkspace.jsx:375-380`
- **What happens:** I compared the preview side by side with the real /p/<token> on world 121. Differences found: (1) Spotlight on 'Supply Chest': the real view shows the '🔦 The Keep ▸ Supply Chest' bar and The Keep glowing. The preview shows neither, because it only adds .spot to the node itself (shots/40-prev-root-spot-chest.png vs 40-real-root-spot-chest.png). (2) Party footprints: the preview draws 3 prints on the root, including a DM-only footstep (t=5) and a hidden-era footstep (t=9); the real view draws 1 (shots/71-prev-t15.png vs 71-real-t15.png). PartyTrail gets raw data.placements. (3) The Party reader in the preview says 'Then on to The Sunken Keep · S5·9 ▸', which is the future footstep at 38, and '🕓 30 – 37 footsteps'; the real sheet has neither. It uses the DM's getTrail and shows the rwhen line. (4) List maps: preview rows show node bodies (e.g. 'ROPE-BODY…'), real rows show titles only. (5) Clicking a Thread to 'Den Guard' navigates the preview into the DM-only 'Den of Secrets' interior. Crumbs, the guard and its body all render (shots/14-prev-guard-in-dm-den.png), while the real deep link 404s. (6) The top chip reads '🕓 37 footsteps' even while scrubbing the past; the real chip reads 'Session 1 · footstep 6 · the past'. (7) Selecting future-only 'Future Tower' in View and switching to Player keeps a reader open: 'Future Tower — Nothing is known of this at 37 footsteps.' Players cannot see that node at all. (8) Clicking the Future Tower thread in the preview also gives 'Nothing is known of this', while the real sheet shows its full body. So the preview hides the leak in the previous finding. (9) Double-clicking a pin with no interior flashes the DM instruction '… give it one from the inspector (＋ Interior map)'; the real view does nothing. The ✍ badge on player markers is also missing.
- **Why it matters:** The preview should render exactly the share API's result, because the DM uses it to check secrecy before a session.
- **Fix:** Best: have the Player posture render PlayerView's map, sheet and trail components from a DM-authenticated preview endpoint (e.g. GET /api/atlas/worlds/:id/preview/maps/:mapId and /nodes/:id) that calls the same functions as share.js. Factor worldOf/allowedTime/walkUp/spotlightTrail and the handlers into a module that takes a world row instead of a token. Smaller patch if that is too big: in player mode pass PartyTrail placements.filter(visible), build the party neighbours from a trail filtered to non-DM steps with start ≤ canon, hide .rwhen and .lsdesc, render the spotlight trail, use momentLabel(previewT ?? canon) in the chip, clear selId on switchMode when the selection is not visible, show a 'players can't reach this place' state when the current map would fail walkUp, and route jump() through the share locate rules.
- **Repro:** Run b1, b2, b3, b5 and b8 in the lane folder. For example, open /w/121/m/389, click 🎭 Player, and compare the .fstep count and the .dmtrail bar with /p/<token> at the same moment.
- **Evidence:** b8.json prevRootPrints (3) vs realRootPrints (1); prevPartyReader 'Then on to The Sunken Keep · S5·9 ▸' vs realPartySheet (none); b3.json prevList vs realList; b5.json prevRootSpots [] vs realRootSpots ['The Keep']; b10.json playerAfterSwitch; b4.json prevNoInteriorFlash; shots 14-, 40-, 71-, 72-
- **Re-proved:** Checked the code at every cited line, then reproduced on clone 144. (1) AtlasWorkspace has no .dmtrail. .spot is added only when world.spotlightNodeId === p.node.id (1051/1063). The preview's dmtrail count was 0. (2) PartyTrail receives the unfiltered data?.placements (1058). After adding a DM-only Party footstep at t=5, the preview drew prints ['Session 1 · footstep 5','Session 2 · footstep 10'] and the real view drew one. (3) The reader's party links use the DM getTrail (partyNeighbors(trail,…
- **Also found as:** "The 🎭 Player posture says it shows 'exactly what the share link shows', but it…" (client-dead); "Player-posture preview is not faithful: footprints come from unfiltered placeme…" (confusing-code); "The 🎭 Player preview shows a lifespan line the real Player View never shows" (journey)

### C021 · One class name, several meanings (legend, open, tgear, factions, fname, muted, env-status); two caused real bugs

Confusing · medium · effort s · found by `scss-dead`

- **Where:** The `.tgear` buttons are at AtlasWorkspace.jsx:1296 (⤢), 1312 (↩), 1318-1320 (⏳) and 1321 (⚙), not 1314/1320/1323. The `.factions` div is at Dashboard.jsx:211; line 207 is the `.fname` h2.
- **Files:** `client/src/styles/atlas.scss:231`, `client/src/styles/atlas.scss:754`, `client/src/styles/atlas.scss:78`, `client/src/styles/atlas.scss:647`, `client/src/pages/AtlasWorkspace.jsx:1296`, `client/src/pages/AtlasWorkspace.jsx:1314`, `client/src/pages/AtlasWorkspace.jsx:1320`, `client/src/pages/AtlasWorkspace.jsx:1323`, `client/src/styles/dashboard.scss:84`, `client/src/styles/dashboard.scss:63`, `client/src/styles/archive.scss:74`, `client/src/styles/atlas.scss:105`, `client/src/styles/shell.scss:192`, `client/src/styles/main.scss:348`
- **What happens:** `.legend` is both the map's category-filter chip bar and the ? popover's colour key (this caused the help-popover overlap). `.open` is both the passive 'has an interior' ◎ glyph and the Player View's clickable go-inside button (this caused the hidden ◎ on desktop). `.tgear` ('timeline gear') is the class of four different timebar buttons: ⤢ focus expand (1296), ↩ back to canon (1314), ⏳ ghosts (1320) and ⚙ config (1323). `.factions` (dashboard.scss:84, Dashboard.jsx:207) holds the featured card's action buttons, not D&D factions. `.fname` is the featured world's name in dashboard.scss:63 and a folder name in archive.scss:74. `.muted` is italic in the workspace (atlas.scss:105) and not on shell pages (shell.scss:192). `.env-status` (main.scss:348) styles the Admin panel's database-status box.
- **Why it matters:** Class names say what the element is, and one name means one thing.
- **Fix:** Rename: help key → .hkey; PV button → .enter only; timebar buttons → .tbtn (with .tfocus/.tghosts/.tcfg modifiers); .factions → .factions-row or .fcta; archive .fname → .foldername; .env-status → .db-status. Pick one .muted meaning in the shared primitives.
- **Repro:** grep -n 'className="legend"\|className="open\|className={`tgear\|className="tgear\|className="factions"' client/src/pages/*.jsx
- **Re-proved:** `.legend` is used by the chip bar at AtlasWorkspace.jsx:1184 and by the help popover key at 1241. The override at atlas.scss:754 (`.atlas .helppop .legend`) does not reset the base `position:absolute;left:10px;bottom:10px` from L231. Live on my clone, the help popover's .legend computed to position:absolute and width 118px, and the screenshot shows the 'Colours:' text drawn over the other help lines (shots/help.png), so the overlap is present now. `.open` is a passive span in AtlasWorkspace.jsx… _(partly — the corrected location is used above)_

### C038 · Misleading names and hidden logic: nodeLinks holds facts, searchIndex doubles as a visibility index, dropNode means two things, selection parsed from a className

Confusing · low · effort s · found by `confusing-code`

- **Where:** client/src/pages/AtlasWorkspace.jsx, client/src/components/Regions.jsx, client/src/styles/atlas.scss
- **Files:** `client/src/pages/AtlasWorkspace.jsx:50`, `client/src/pages/AtlasWorkspace.jsx:55`, `client/src/pages/AtlasWorkspace.jsx:305`, `client/src/pages/AtlasWorkspace.jsx:2178`, `client/src/pages/AtlasWorkspace.jsx:1471`, `client/src/components/Regions.jsx:96`, `client/src/pages/AtlasWorkspace.jsx:38`, `client/src/pages/AtlasWorkspace.jsx:51`
- **What happens:** nodeLinks holds links, backlinks AND facts, and reloadLinks reloads facts (50, 271-278, 389-393, 1384, 1488). searchIndex (55) is also the visibility index for the player preview's thread pruning (482, 762). loading (35, world) vs loadState (30, map) don't say which. `data` is the map payload and is shadowed by parameters named data (390, 458, 574). dropNode is 'create a node at x,y' in the workspace (305) and 'leave the selection out of the message' in ForgePanel (2178). clamp (18) is clamp-to-0..100. Regions decides selection by regex over the className string (Regions.jsx:96 /\bsel\b/.test(it.cls)). The ambience 'Make it' button reads its input via e.currentTarget.parentElement.previousSibling.value (1471). JSX IIFEs hide logic at 1082, 1252-1269, 1383-1386, 1391-1402; PlayerView repeats `x >= (tl?.current ?? x) ? null : x` inline (217, 328, 329). Picker comments are incomplete: picker kinds omit 'backdrop-timed' (38), nodePicker values omit 'place-here' (51). CSS names: open2, ipin, pmark, stb, rk, lx, ex, sv (atlas.scss:80, 467, 503, 623, 317, 193, 394, 208).
- **Why it matters:** Names say what they hold; no DOM traversal or class-string parsing for state.
- **Fix:** Rename nodeLinks→nodeDetail, reloadLinks→reloadNodeDetail, searchIndex→nodeIndex, loading→worldLoading, data→mapData, ForgePanel dropNode→omitSelection, clamp→clampPct; pass `selected` to Regions items; control the ambience input with state; extract the IIFEs to helpers (tickSteps, <PartyNeighbors>, toViewT); rename the cryptic CSS classes.
- **Repro:** Read the listed lines.
- **Re-proved:** Checked every sub-claim in AtlasWorkspace.jsx. nodeLinks (line 50) is {out, in, facts}. reloadLinks (271-272) reloads facts, and factAdd/Patch/Delete (387-393) call it. Facts are read as nodeLinks.facts at 1384 and 1488. searchIndex (55) is loaded for player mode at 482 and used as a visibility map at 762. loading (35) is set around the world fetch (231, 242, 876); loadState (30) tracks the map fetch (158, 162, 166, 1033). Parameters named `data` shadow the map payload at 390 (factPatch), 458 (…

### C039 · Workspace and Player View copy-paste pin markup, pointer→plane math and outline constants; STYLE_KEYS is imported but a third hardcoded list is used

Confusing · low · effort m · found by `confusing-code`

- **Where:** client/src/pages/AtlasWorkspace.jsx:1061-1084, 2045; client/src/pages/PlayerView.jsx:224-251
- **Files:** `client/src/pages/AtlasWorkspace.jsx:12`, `client/src/pages/AtlasWorkspace.jsx:2045`, `client/src/pages/AtlasWorkspace.jsx:1061`, `client/src/pages/PlayerView.jsx:224`, `client/src/components/Regions.jsx:28`, `server/routes/atlas.js:52`, `client/src/pages/ImageManager.jsx:12`
- **What happens:** Pin className/markup duplicated between AtlasWorkspace 1061-1084 and PlayerView 224-251. Pointer→% of plane computed six ways: AtlasWorkspace 645-646, 715, 843-844, 854-855; PlayerView 104-107; Regions.jsx:55-58. Outline style keys exist in Regions.jsx:28 and server/routes/atlas.js:52, and the Inspector hardcodes the list again with labels (AtlasWorkspace.jsx:2045) while STYLE_KEYS is imported at line 12 and never used there. MAX_CORNERS 200 (utils/geometry.js:34) mirrors atlas.js:39 by hand. usesOf is identical in AtlasWorkspace 2076-2078 and ImageManager.jsx:12-14. The 'store an image' pipeline (filename, R2 vs base64, INSERT) exists three times: image-base64.js:41-74, forge/contract.js paintAndStore, atlas.js clone 186-195.
- **Why it matters:** Shared pieces live in one module.
- **Fix:** components/Pin.jsx; export toPlanePct(e, el) from MapPlane.jsx; drive the Inspector toggles from STYLE_KEYS plus a label map in Regions.jsx; utils/images.js usesOf; server/lib/storeImage.js used by upload, paint and clone.
- **Repro:** grep -n STYLE_KEYS client/src -r → AtlasWorkspace.jsx:12 import only
- **Re-proved:** `grep -rn STYLE_KEYS` over client/src, server and e2e shows it defined at Regions.jsx:28 (and used there at :120), defined again at server/routes/atlas.js:52 (used at :57), and only imported at AtlasWorkspace.jsx:12. AtlasWorkspace never uses it; the Inspector hardcodes the fill/stroke/grow/glow/pop list with labels at AtlasWorkspace.jsx:2045. The pin className and markup are near-identical at AtlasWorkspace.jsx:1061-1084 and PlayerView.jsx:224-251. The pointer-to-% math is repeated at AtlasWor…

### C040 · Time vocabulary collides: `now` is the DM lens in the workspace but canon in EraScrub; `current` means canon; the effective moment has three names

Confusing · low · effort s · found by `confusing-code`

- **Where:** client/src/pages/AtlasWorkspace.jsx:39, 563, 826, 1058, 1392; client/src/components/EraScrub.jsx:92, 96; client/src/pages/PlayerView.jsx:142
- **Files:** `client/src/pages/AtlasWorkspace.jsx:39`, `client/src/pages/AtlasWorkspace.jsx:826`, `client/src/components/EraScrub.jsx:92`, `client/src/pages/PlayerView.jsx:142`, `server/forge/mind.js:107`
- **What happens:** AtlasWorkspace `now` = the DM's local lens (39); EraScrub prints 'now · …' and '⦿ Now' for CANON (EraScrub.jsx:92, 96) and PlayerView's tooltip says 'The current moment' for canon (173). `tl.current` / timeline_current_time mean canon (472), and the Forge digest calls the same field `canon` (server/forge/mind.js:107). The 'moment being shown' is computed inline four times as `mode === 'player' ? (previewT ?? canon) : now` (563, 826, 1058, 1392); the one named copy is `bdMoment` (826, 'backdrop moment') though it also drives facts and the ghost reader (1360, 1384); PlayerView names the same thing `tEff` (142) and the player scrub `viewT`, the preview scrub `previewT`.
- **Why it matters:** lens / canon / shownAt used consistently.
- **Fix:** Rename now→lens, tl.current→canon (map in atlasService.getWorld), bdMoment/tEff→shownAt; compute shownAt once and use it at 563, 1058, 1392.
- **Repro:** grep -n "previewT ?? canon" client/src/pages/AtlasWorkspace.jsx
- **Re-proved:** AtlasWorkspace.jsx:39 declares `now`, commented 'the DM's viewing moment (local lens — NOT what players see)'. Line 472 has `const canon = tl?.current ?? 0`. EraScrub.jsx:92 renders `now · ${momentLabel(canon, …)}` and :96 renders '⦿ Now' (title 'Back to the present'), both meaning canon. The PlayerView.jsx:173 tooltip reads 'The current moment, set by your DM'. server/forge/mind.js:107 maps timeline_current_time to `canon:`. `mode === 'player' ? (previewT ?? canon) : now` appears four times, a…

### C042 · UI preferences and 'last world' state are hand-rolled many times with inconsistent encodings, guards and magic numbers

Confusing · low · effort s · found by `confusing-code`

- **Where:** client/src/pages/AtlasWorkspace.jsx:46-121, 199-203, 442-451, 484-558, 626-631, 687-692, 774-789; client/src/services/worldService.js:46-97
- **Files:** `client/src/pages/AtlasWorkspace.jsx:66`, `client/src/pages/AtlasWorkspace.jsx:495`, `client/src/pages/AtlasWorkspace.jsx:626`, `client/src/services/worldService.js:51`, `client/src/pages/Dashboard.jsx:100`, `client/src/App.jsx:48`
- **What happens:** Six copy-paste toggle setters (66-70, 199-203, 442-446, 447-451, 484-488, 489-493) plus inline ones (1320, 325) with encodings 'on'/'off' and 'open'/'closed'; writes are try-wrapped but all initial reads (47, 62-65, 75-76, 84-96) and two writes (325, 1320) are not. Three copy-paste resize handlers (495-553) with parallel refs (116-121) and unnamed bounds 160/420/0.4, 280/640/0.55, 300/720/0.6, defaults 230/310 repeated at 87/91/116/120/513/533, Forge column 340px (1007), breakpoint 700 (97-99). Four outside-click effects (626-631, 687-692, 774-779, 781-789). 'Where was I' is stored three ways: current_world (full object, only .id ever read: Dashboard.jsx:100, ImageManager.jsx:58), current_world_id (worldService.js:51-57; getCurrentWorldId has no callers) and atlas_last_location (App.jsx:48). Entering a world through '/' or a URL sets only atlas_last_location (AtlasWorkspace.jsx:163), so Dashboard's 'Pick up where you left off' (Dashboard.jsx:206) can name a different world than '/' resumes.
- **Why it matters:** One hook per pattern, named constants, one 'last location' key.
- **Fix:** hooks/usePersistentState(key, default) with try/catch on read and write; hooks/useColumnResize({key, min, max, maxFrac, fallback, edge}); hooks/useDismiss(ref, open, onClose); a LAYOUT constants object; drop current_world/current_world_id in favour of atlas_last_location (Dashboard reads its worldId).
- **Repro:** grep -rhno "localStorage\.\(getItem\|setItem\|removeItem\)([^,)]*" client/src | sort | uniq -c
- **Re-proved:** The six toggles match the cited lines exactly: togglePrints 66-70, toggleForge 199-203, toggleLabels 442-446, toggleGrid 447-451, toggleRail 484-488, toggleInsp 489-493. The prints/labels/grid toggles store 'on'/'off'; the forge/rail/inspector toggles store 'open'/'closed'. Those setters wrap their writes in try, but line 325 (setDrawKind) and line 1320 (the ghosts ⏳ toggle, inline) call localStorage.setItem with no try. No initial read is wrapped: lines 47, 62-65, 75-76, 84, 86, 90 and 94 (lin…

### C044 · Time resolution (presence and 'latest-starting row covering t') is implemented about 12 times across client and server

Confusing · low · effort s · found by `confusing-code`

- **Where:** client/src/utils/moment.js:16-21 (partyWhere, presence test on line 18), plus a partial copy in partyNextFrom at moment.js:28-32
- **Files:** `client/src/pages/AtlasWorkspace.jsx:562`, `client/src/pages/AtlasWorkspace.jsx:827`, `client/src/pages/AtlasWorkspace.jsx:864`, `client/src/pages/PlayerView.jsx:148`, `server/routes/share.js:163`, `server/routes/share.js:204`, `server/routes/share.js:234`, `server/routes/share.js:309`
- **What happens:** Presence: AtlasWorkspace presentAt (562), PlayerView present (143-144), PartyTrail lastAlive (PartyTrail.jsx:16), moment.js partyWhere (36-37), share.js PRESENT (42-43), spotlightTrail alive (76), and three identical windowed interval-condition builders plus three identical clamp-to-envelope expressions in one handler (share.js:163-166/204-207/234-237 and 193-194/214-215/250-251). Covering-row pick: AtlasWorkspace backdrop (827-835) and resolveFact (864-869), PlayerView backdrop (148-155), share.js backdrop SQL (152-156) and fact SQL (309-313). They agree today (client null=-Infinity sort equals SQL DESC NULLS LAST), but each rule change has to be made in every copy by hand.
- **Why it matters:** One implementation per side.
- **Fix:** Client: utils/timeline.js with isPresent(p, t) and pickCovering(rows, t), used by AtlasWorkspace, PlayerView, PartyTrail, moment.js. Server: in share.js, intervalConds(ivs, alias, firstParam) and clampToEnvelope(row, lo, canon) helpers; keep PRESENT.
- **Repro:** Read the listed lines side by side.
- **Re-proved:** Read every cited line at HEAD 32ef89c. AtlasWorkspace.jsx:562 presentAt, :827-835 activeBackdropUrl (filter covering rows, sort start DESC with null=-Infinity, then id DESC), :864-869 resolveFact with the same sort. PlayerView.jsx:143-144 present, :148-155 backdropUrl with the same sort again. PartyTrail.jsx:16 lastAlive. share.js:42-43 PRESENT, :76 spotlightTrail alive, :152-156 backdrop SQL and :309-313 fact SQL (both ORDER BY start_time DESC NULLS LAST, id DESC, which matches the client sort… _(partly — the corrected location is used above)_

### C050 · z-index is a set of magic numbers with no scale; `.apop` z-index:30 does nothing inside the Map ▾ and ? popovers

Confusing · low · effort s · found by `scss-dead`

> **Second pass — see also:** The re-proof is wrong for the ? popover: the timeline ⚙ panel (.tlcfg, z 8) paints over it (z 7) and also ignores outside clicks. With the proposed scale, keep $z-panel below $z-popover. → **P117** in [WP-28](WP-28-one-click-does-one-thing-and-the-map-s-cues-tell.md)

- **Where:** Details that are wrong: (1) There are 54 z-index declarations, not 45 (`grep -o z-index *.scss | wc -l`: atlas 38, shell 7, dashboard 6, archive 3). The list of distinct values is correct. (2) The shell.scss `z-index: 60` is on line 203; the `.modal-back` rule starts at 195. (3) The overpaint list is only right for the Map ▾ menu (effective z 5): the legend (z5, later in the DOM at L1184), mp-controls (6), railtoggle/insptoggle (6) and drawhud (7) can all paint over it. It is wrong for the ? popover (effective z 7): legend, mp-controls and the toggles are lower, and drawhud (L1204) comes before helpwrap in the DOM, so none of them paint over it. `.markbtn` only renders in PlayerView.jsx:283, which has neither menu, so it never overlaps either one.
- **Files:** `client/src/styles/atlas.scss:62`, `client/src/styles/atlas.scss:282`, `client/src/styles/atlas.scss:294`, `client/src/styles/atlas.scss:109`, `client/src/styles/shell.scss:195`
- **What happens:** There are 45 z-index declarations across atlas/shell/dashboard/archive with no shared scale (values 0-9, 12, 15, 20, 25, 30, 40, 50, 55, 60, 70, 75, 80). `.apop` declares z-index:30 (L282), but the Map ▾ menu lives inside `.toolbar` (position:absolute; z-index:5, L62) and the ? popover inside `.helpwrap` (z-index:7, L294). Both parents are stacking contexts, so the menus stack at 5 and 7 among the stage overlays. The legend (5, later in the DOM), mp-controls (6), railtoggle/insptoggle (6) and drawhud/markbtn (7) would paint over them if they overlapped. The same `.modal-back` class is z 50 in atlas.scss:109 and z 60 in shell.scss:195.
- **Why it matters:** A documented layer scale (plane < stage chrome < panels < popovers < modals < toasts), with popovers not nested in low-z parents.
- **Fix:** Add named layers in the token partial ($z-plane:1…, $z-chrome:5, $z-panel:12, $z-popover:30, $z-modal:50, $z-toast:80) and use them everywhere. Drop the z-index from `.toolbar`/`.helpwrap` (position alone suffices), or portal the popovers to the .atlas root.
- **Repro:** node scratch lanes/scss-dead z-index listing (see notes for the full map).
- **Re-proved:** I checked the code and did not use the live site; CSS stacking rules settle this one. The core claim holds. atlas.scss:62 is `.toolbar{position:absolute;...;z-index:5}`, atlas.scss:294 is `.helpwrap{position:absolute;...;z-index:7}` and atlas.scss:282 is `.apop{position:absolute;...;z-index:30}`. In AtlasWorkspace.jsx the Map ▾ `<div className="apop">` (L1137) sits inside `.toolbar` (L1118) via `.mapmenu`, which is position:relative with no z-index. The ? `<div className="apop helppop">` (L1233… _(partly — the corrected location is used above)_

### C068 · API shapes differ per route: camelCase body on spotlight/forge context, mixed-case upload body, three shapes for the world timeline, many response envelopes

Confusing · low · effort m · found by `confusing-code`

- **Where:** server/routes/*.js, client/src/services/*.js
- **Files:** `server/routes/atlas.js:120`, `server/routes/forge.js:138`, `server/routes/image-base64.js:11`, `server/routes/atlas.js:95`, `server/routes/worlds.js:42`, `server/routes/share.js:118`, `server/routes/admin.js:32`, `server/routes/forge.js:40`
- **What happens:** Request bodies are snake_case (node_id, start_time, shape_kind, from_node_id) except POST /atlas/worlds/:id/spotlight {nodeId} (atlas.js:120, atlasService.js:15) and forge chat context {mapId,nodeId} (forge.js:138,143); the image upload mixes imageData/originalName with world_id/alt_text in one body (image-base64.js:11). The world timeline comes back as {enabled,min,max,current,unit} (atlas.js:95-96), as timelineEnabled + timelineSettings{minTime,maxTime,currentTime,timeUnit} (worlds.js:42-48) and as {enabled,current,unit} (share.js:118). Envelopes: {world}, {maps}, {nodes}, {steps}, unwrapped map/impact, {ok:true}, {id}, {nodeId,placementId}, {message:'World created successfully', world} (worlds.js:135-147), {success:true,…} (admin.js:32, 64). forge messages return created_at next to batchId (forge.js:40). World CRUD is split: create/list/delete in /api/worlds, rename in /api/atlas PATCH (Dashboard.jsx:119, 132, 144) with different rules (POST refuses duplicate names 409, worlds.js:118-125; PATCH and clone do not).
- **Why it matters:** Documented conventions: snake_case bodies, camelCase responses, one timeline shape.
- **Fix:** Accept node_id on spotlight (keep nodeId for a release); make /api/worlds return the atlas timeline shape; move world rename/description into worlds.js (or all world routes into atlas.js) with one name rule; write the conventions into CLAUDE.md.
- **Repro:** Read the listed lines.
- **Re-proved:** atlasService.js:15 sends {nodeId} and atlas.js:120 reads nodeId. forge.js:138 and :143 read context.mapId and context.nodeId. image-base64.js:11 destructures imageData, originalName, world_id, alt_text and tags. There are three timeline shapes: atlas.js:95-96 {enabled,min,max,current,unit}, worlds.js:42-48 (and again at :88-94) timelineEnabled + timelineSettings{minTime,maxTime,currentTime,timeUnit}, and share.js:118 {enabled,current,unit}. worlds.js:135-147 returns {message:'World created succ…

### C069 · Server error handling and ownership checks are copy-pasted with different behaviour (wrap x4, ownsWorld x3+, 404/400/403 for the same failure, 403 for expired tokens)

Confusing · low · effort m · found by `confusing-code`

> **Second pass — see also:** Forge Keep and Refuse answer 200 {ok:true} when no batch matched, while Allow and Discard answer 404. Check rowCount in those two routes when you unify the status codes. → **C096** in [WP-25](WP-25-the-forge-nothing-public-before-keep-and-unmake.md)

- **Where:** server/routes/atlas.js:13-20, share.js:13, forge.js:26-32, voice.js:20-26, images.js:167/236, middleware/auth.js:23-45, client/src/services/http.js:22-31
- **Files:** `server/routes/atlas.js:13`, `server/routes/forge.js:26`, `server/routes/voice.js:20`, `server/routes/share.js:13`, `server/routes/atlas.js:730`, `server/routes/images.js:167`, `server/middleware/auth.js:42`, `client/src/services/http.js:23`
- **What happens:** wrap() is defined four times; forge.js:27 and voice.js:21 send err.message to the client on 500, atlas.js:14 and share.js:14 don't. worlds/images/imageFolders/image-base64/auth/admin/setup use per-route try/catch instead. ownsWorld is defined in atlas.js:17, forge.js:29, voice.js:23 and re-inlined in worlds.js:160, images.js:21, image-base64.js:18, imageFolders.js:19. Not-yours is 404 almost everywhere, but POST /atlas/links answers 400 'Invalid link' (atlas.js:730-731; verified live: 400) and images PUT/DELETE answer 403 based on uploaded_by with an admin-role bypass (images.js:167, 236), unlike every world-scoped route. Expired/invalid tokens return 403 (middleware/auth.js:42-45), so the client regexes the message text to tell a dead token from a permission error (http.js:23 TOKEN_MSG).
- **Why it matters:** One wrap/ownsWorld module; one status per failure; 401 for authentication failures.
- **Fix:** server/lib/route.js exporting wrap, ownsWorld, notFound; images.js checks world ownership; auth middleware returns 401 for missing/invalid/expired tokens, then drop TOKEN_MSG from http.js.
- **Repro:** grep -n "const wrap\|async function ownsWorld" server/routes/*.js
- **Re-proved:** `grep -n 'const wrap\|async function ownsWorld'` finds wrap at atlas.js:13, share.js:13, forge.js:26 and voice.js:20. forge and voice send err.message to the client on a 500; atlas and share send only 'Server error'. ownsWorld is defined at atlas.js:17, forge.js:29 and voice.js:23. The same query is written inline at worlds.js:160-161, images.js:21-22, image-base64.js:18-19 and imageFolders.js:19-20. atlas.js:729-731 answers 400 'Invalid link' when you don't own the world. images.js:167 and :23…

### C070 · Image pipeline naming: the only upload route is called 'base64' and writes to R2; base64 storage is 'legacy' in code but 'deliberate fallback' in the README

Confusing · low · effort m · found by `server-dead`

- **Where:** server/routes/image-base64.js; server/server.js:98-99; client/src/services/imageServiceBase64.js
- **Files:** `server/routes/image-base64.js:8-146`, `server/server.js:98-99`, `server/utils/imageUrl.js:4`, `server/config/schema.sql:74`, `server/forge/contract.js:198`, `client/src/services/imageServiceBase64.js`, `README.md`
- **What happens:** /api/images-base64/upload is the only upload path and stores to R2 whenever R2 is configured; 'base64' describes the transport, not the storage. The client's only image service, imageServiceBase64, also wraps /api/images list/update/delete. Code comments call base64-in-Postgres rows 'legacy' (image-base64.js:48, schema.sql:74, imageUrl.js:4, contract.js:198), while README says it is 'the deliberate fallback… Both paths are live'. storage_key NULL is described as 'legacy base64 rows' (schema.sql:74) but also marks every clone row. The serve route's redirect branch (image-base64.js:114-121) has no remaining generator: all three writers (image-base64.js:48-56, contract.js:239-244, atlas.js:192) set a /serve/ path only when bytes are stored, and its documented caller (event tooltip blobs) is gone.
- **Why it matters:** One image router and one image service named for the resource, and one consistent word for base64 storage.
- **Fix:** Move the upload into images.js as POST /api/images (keep the old path as an alias for a while). Rename imageServiceBase64 to imageService. Pick one term ('Postgres fallback') and use it in the comments. Delete the redirect branch at image-base64.js:114-121, or add a comment naming what still needs it.
- **Repro:** grep -rn "legacy" server/routes/image-base64.js server/utils/imageUrl.js server/forge/contract.js server/config/schema.sql; compare with README.md 'Notes'.
- **Re-proved:** images.js has no POST (only GET /, GET /:id, PUT /:id, DELETE /:id), so /api/images-base64/upload (image-base64.js:9) is the only upload route, and it writes to R2 when r2Enabled (lines 48-56). The client has no other image service: imageServiceBase64.js posts to /api/images-base64/upload and also calls GET/PUT/DELETE on /api/images (lines 60, 70, 80, 90). 'legacy' appears at image-base64.js:48, schema.sql:74, imageUrl.js:4 and contract.js:198, while README.md:45-47 says 'deliberate fallback...…

### C083 · A newcomer can't tell which stylesheet styles what; atlas.scss is a 775-line changelog and the per-page imports imply isolation that doesn't exist

Confusing · low · effort l · found by `scss-dead`

- **Where:** There are 19 `// ----` section markers in atlas.scss (grep -c gives 19), not 20. main.scss is not entirely headerless: L1 reads `// Base styles`, but it does not say which pages it serves.
- **Files:** `client/src/styles/atlas.scss:2`, `client/src/styles/atlas.scss:205-708`, `client/src/styles/main.scss:1-25`, `client/src/styles/archive.scss:1`, `client/src/pages/ImageManager.jsx:8`, `client/src/App.jsx:5-14`, `CLAUDE.md`
- **What happens:** atlas.scss styles AtlasWorkspace, PlayerView and 5 shared components (MapPlane, Regions, PartyTrail, AudioClip, EraScrub), but it is organised by date of feature, not by component. `.atlas {` is reopened 9 times, and 20 '// ----' sections are appended in commit order (additions, postures, eras, backdrops, image pins, player markers, forge, lantern, collapsed pins, forge-again, PV nav, voices, audio, party, tree, outlines). `.pin` rules sit on 31 lines across ~12 places (L73-82, 184, 467-481, 503-505, 574-575, 607-618, 623-626, 647, 681-682, 766, 774). `.forge` sits in 4 places (L520-564, 580-583, 632-641), `.trow` in 2 (L33-37, L695-702), `.tlcfg` in 2. It uses a dense one-line style (18 lines over 160 chars) while shell, dashboard and archive use expanded SCSS. main.scss has no header and mixes a global reset, a light theme and five pre-redesign pages, and its 'Import component styles' comment (L25) introduces nothing. archive.scss styles ImageManager.jsx, served at /worlds/:id/images, which the UI calls 'The Archive' under root class `.arch`: four names for one page. Every page imports 'its' sheet, but App.jsx imports all pages eagerly, so all five sheets ship in one bundle and apply everywhere. Scoping relies solely on the root classes.
- **Why it matters:** One file (or partial) per surface, sections by component, and a header saying which pages each sheet serves.
- **Fix:** Split atlas.scss into partials by component (_tokens, _shell-chrome, _plane-pins-regions, _timebar-eras, _inspector, _reader-sheet, _forge, _player-view, _audio) and gather each component's rules in one place. Add a 2-line header to every sheet naming its pages. Rename archive.scss→imagemanager.scss or ImageManager.jsx→Archive.jsx so one name wins. Note in CLAUDE.md that scoping is by root class only.
- **Repro:** grep -n "^\.atlas {\|^// ----" client/src/styles/atlas.scss; grep -n "\.pin\b" client/src/styles/atlas.scss | cut -d: -f1 | tr '\n' ' '; awk 'length>160' client/src/styles/atlas.scss | wc -l
- **Re-proved:** Checked on HEAD 32ef89c. atlas.scss has `.atlas {` at lines 2, 206, 275, 348, 384, 437, 451, 466 and 502, plus `.atlas{` at 680. The `.pin` rules sit on exactly 31 lines: 73, 76-82, 184, 467, 503, 505, 574-575, 607-618, 623-626, 647, 681-682, 766 and 774. `.forge` appears at 523-564, 580 and 633-641. `.trow` appears at 33-37 and 695-702. `.tlcfg` is defined at 178-183 with width 250px, then again at 385 with width 320px, which overrides the first. `awk 'length>160'` finds 18 lines in atlas.scss… _(partly — the corrected location is used above)_

### C084 · AtlasWorkspace.jsx is a 2,408-line / 135 KB file; concrete split plan (what moves where, and in what order)

Confusing · low · effort l · found by `confusing-code`

- **Where:** There are seven sub-components below the main component, not eight: MapTree 1684, DeleteImpact 1754, TimelineConfig 1773, Inspector 1815, ImagePicker 2062, NodePicker 2135, ForgePanel 2170.
- **Files:** `client/src/pages/AtlasWorkspace.jsx:22`, `client/src/pages/AtlasWorkspace.jsx:884`, `client/src/pages/AtlasWorkspace.jsx:1063`, `client/src/pages/AtlasWorkspace.jsx:1815`, `client/src/pages/AtlasWorkspace.jsx:2165`
- **What happens:** The main component (22-1678) holds 51 useState, 20 useRef, 17 useEffect and 55 `mode ===/!==` posture checks; its render body is 794 lines (884-1677). Longest lines: 1063 (433 chars, pin className) and 1815 (434 chars: Inspector with 39 props, 27 of them callbacks, wired over 1481-1505). Where it hurts most: (1) the render body mixes all three postures, so any change needs reading 55 posture checks; (2) the Inspector prop wall; (3) 51 independent state slots whose reset rules are scattered (the outline-tool bug above comes from this). Eight sub-components already live at the bottom of the file and depend only on their props.
- **Why it matters:** A maintainer can open the timebar, the reader or the Forge panel without scrolling 2k lines.
- **Fix:** Pure moves first (no behaviour change): ForgePanel 2165-2406 → components/atlas/ForgePanel.jsx; Inspector 1815-2058 → components/atlas/Inspector.jsx (group props into node/outline/voice/facts/links objects); ImagePicker 2060-2132 and NodePicker 2134-2163 → components/atlas/Pickers.jsx; MapTree 1680-1752, TimelineConfig 1773-1813, DeleteImpact 1754-1771 → own files. Second: Timebar (render 1248-1332 + focus math 800-824) → components/atlas/Timebar.jsx; Reader (1335-1420 + readerLinks 759-764 + resolveFact 864-869) → components/atlas/Reader.jsx; space panel 1424-1479 → SpacePanel.jsx; top bar 886-1001 → WorkspaceTopBar.jsx; modals 1555-1668 → components/atlas/modals/; pin markup 1061-1084 shared with PlayerView 224-251 → components/Pin.jsx. Third, hooks: usePersistentState (47-96, 199-203, 442-451, 484-493, 1320), useColumnResize (495-558 + refs 116-121), useDismiss (626-631, 687-692, 774-789), useNodeAutosave (358-373, 633-638); utils/timeline.js for presentAt/activeBackdropUrl/resolveFact (562-563, 826-835, 864-869). e2e selectors to keep: .atlas, .timebar, .reader, .toolbar, .mode, .mp-world, .mp-viewport, .pview, .rtrail, .sbody, .lbl.
- **Repro:** wc -l; awk length; grep -c useState over lines 22-1678; grep -o "mode [!=]== '" | wc -l
- **Evidence:** 51 useState / 20 useRef / 17 useEffect in 22-1678; 55 posture checks; lines >400 chars: 1063, 1815
- **Re-proved:** The file is 2,408 lines and 135,291 bytes (wc -l / wc -c). The main component runs from line 22 to 1678, and its render return runs from 884 to 1677. Within 22-1678 there are 51 useState(, 20 useRef( and 17 useEffect( calls. The file has 55 matches for `mode [!=]== '`, all inside the main component. Only two lines are over 400 chars: 1063 (433) and 1815 (434). Inspector declares 39 props, 27 of them on*, and is wired at 1481-1505. I spot-checked the split-plan ranges and they match the code, co… _(partly — the corrected location is used above)_

### C090 · Same job, many looks: 8 'on' treatments for toggles, `.tool.on` doubling as the primary button, 8 close-button classes, 5 section-label styles

Confusing · low · effort m · found by `scss-dead`

- **Where:** Workspace + Player View controls · client/src/styles/atlas.scss
- **Files:** `client/src/styles/atlas.scss:27`, `client/src/styles/atlas.scss:64`, `client/src/styles/atlas.scss:92`, `client/src/styles/atlas.scss:94`, `client/src/styles/atlas.scss:228`, `client/src/styles/atlas.scss:292`, `client/src/styles/atlas.scss:378`, `client/src/styles/atlas.scss:622`, `client/src/styles/atlas.scss:750`, `client/src/styles/atlas.scss:758`, `client/src/pages/AtlasWorkspace.jsx:974`, `client/src/pages/AtlasWorkspace.jsx:1650`, `client/src/pages/AtlasWorkspace.jsx:2396`, `client/src/pages/PlayerView.jsx:393`
- **What happens:** The selected state of a toggle is drawn eight ways. `.mode button.on` is a solid accent with #08131f text (L27). `.chip.on` is solid accent (L92). `.kindsel button.on` is solid accent with #0d0f13 text (L750). `.visseg button.on` is a #5b9bd530 tint (L378). `.apop-row button.on` is a tint plus border (L292). `.strow button.on` is a border plus outline (L622). `.stoggles button.on` is a color-mix fill (L758). `.gfilter button.on` is a border only (L228). `.tool.on` (a 2px outline, L64) means 'pressed' on the toolbar ('＋ Add node' while placing), yet it is also a static class on 13 action buttons used as the primary style: Copy link / Create share link (AtlasWorkspace.jsx:974/983), Try again (1030), Save/Rename (1650/1664), timeline Save / Next session (1785/1807), Forge Allow/Keep/Save the mind/Send (2287/2294/2355/2396), and Player View 'Look inside' / 'Place it' (PlayerView.jsx:334/393). Meanwhile `.btn.primary` (L94) is a second primary style (3 uses). Close buttons: `.rclose`, `.sclose`, `.mclose`, `.lbclose`, `.modal-head button`, `.fhead .x`, `.lx`, `.ex`. Section labels: `.isect`, `.rk`, `.fsect`, `.tsect`, `.kicker`. The round category badge `.ic` is re-declared in 9 containers.
- **Why it matters:** One primary-button style, one pressed-toggle style, one close button, one section label.
- **Fix:** Define primitives once in atlas.scss (.btn / .btn.primary / .btn.danger, .seg > button.on, .xclose, .klabel, .ic + size modifiers). Switch the 13 static `tool on` action buttons to `btn primary`, keeping `.tool.on` only for a pressed tool. Delete the per-container copies.
- **Repro:** grep -rno 'className="tool on[^"]*"' client/src/pages client/src/components | wc -l → 13; grep -n "button.on\|\.on{" client/src/styles/atlas.scss
- **Re-proved:** grep -rno 'className="tool on[^"]*"' returns exactly 13. They are AtlasWorkspace.jsx:974 Copy link, 983 Create share link, 1030 Try again, 1650 Save, 1664 Rename, 1785 Save, 1807 ＋ Next session, 2287 Allow, 2294 Keep, 2355 Save the mind and 2396 Send, plus PlayerView.jsx:334 Look inside and :393 'Place it — everyone sees it'. .tool.on is a 2px accent outline (atlas.scss:64), also applied dynamically for pressed toolbar tools (L1119-1134). .btn.primary (L94) is used at 1404, 1846 and 2101. I rea…

### D009 · Legacy maps columns (description, parent_map_id, zoom_level, map_order, is_active) are only copied around, and 'parentMapId' means something else in the API

Dead code · low · effort s · found by `maps`

- **Where:** Clone (atlas.js:199-202) copies only description, not parent_map_id, zoom_level or map_order; those three are copied only in the undo insertMap (atlas.js:586-590). The finder's list of is_active filters also misses atlas.js:275 (trail), share.js:90-91 and share.js:242. Before dropping those filters, run a read-only `SELECT count(*) FROM maps WHERE is_active=false` (and purge those rows if any exist): the legacy soft-delete removed in a140976 may have left inactive rows.
- **Files:** `server/config/schema.sql:78-91`, `server/routes/atlas.js:196-202`, `server/routes/atlas.js:262-265`, `server/routes/atlas.js:586-590`
- **What happens:** maps.description has no UI to read or edit it. Clone and undo copy it, and so do parent_map_id, zoom_level and map_order, which are otherwise unused. maps.is_active is never set to false anywhere (`grep -rn "is_active *= *false\|SET is_active" server` → nothing), so every 'AND m.is_active = true' filter on maps does nothing (atlas.js:196, 264; images.js:69; share.js:54; worlds.js:18, 68; forge/mind.js:95). Meanwhile GET /worlds/:id/maps returns a parentMapId computed from the owner node's first placement, aliased 'parent_map_id', which hides the legacy column of the same name. That will mislead anyone reading the schema.
- **Why it matters:** The schema carries only live columns, and the API's 'parent map' doesn't share a name with a dead column.
- **Fix:** Stop copying description, parent_map_id, zoom_level and map_order in clone and insertMap. Drop the is_active filters on maps. Add ALTER TABLE maps DROP COLUMN IF EXISTS … to schema.sql, or list them under CLAUDE.md's 'Orphaned' note. Rename the SQL alias to owner_map_id.
- **Repro:** grep -rn "parent_map_id\|zoom_level\|map_order" server client/src; grep -rn "is_active" server | grep -i map
- **Evidence:** grep results in this lane (only clone/undo/listing references; no writer of is_active=false)
- **Re-proved:** Confirmed: maps.description has no client reader or editor. grep turns up only Dashboard world descriptions; the map description is used only at atlas.js:199-201 (clone) and :586-590 (undo insertMap). parent_map_id, zoom_level and map_order show up only in schema.sql:84-90 and :102 and in undo insertMap at atlas.js:586-590. Also confirmed: the tree query aliases a computed subquery AS parent_map_id (:262), which reuses the legacy column's name. Not literally true: the claim that the is_active f… _(partly — the corrected location is used above)_

### D010 · Duplicated helpers: an identical Modal component, plural(), usesOf(), and the 'latest start covering t' resolver written three times

Dead code · low · effort s · found by `client-dead`

- **Where:** Dashboard.jsx / ImageManager.jsx / AtlasWorkspace.jsx / PlayerView.jsx
- **Files:** `client/src/pages/Dashboard.jsx:9`, `client/src/pages/Dashboard.jsx:47-64`, `client/src/pages/ImageManager.jsx:11-14`, `client/src/pages/ImageManager.jsx:506-523`, `client/src/pages/AtlasWorkspace.jsx:2076-2078`, `client/src/pages/AtlasWorkspace.jsx:827-835`, `client/src/pages/AtlasWorkspace.jsx:864-869`, `client/src/pages/PlayerView.jsx:148-155`
- **What happens:** `diff` of Dashboard.jsx:47-64 and ImageManager.jsx:506-523 is empty: the Modal components are identical. plural is duplicated in both files. usesOf is duplicated in ImageManager and AtlasWorkspace's ImagePicker. The timed-override rule (rows covering t, latest start wins, ties by newest id) is copied into activeBackdropUrl, resolveFact and PlayerView's backdropUrl, and CLAUDE.md says the server mirrors the same rule.
- **Why it matters:** One copy of each helper.
- **Fix:** Create components/ShellModal.jsx, and add utils/format.js (plural, usesOf) and a resolveAt(rows, t) in utils/moment.js. Replace the copies.
- **Repro:** diff <(sed -n 47,64p client/src/pages/Dashboard.jsx) <(sed -n 506,523p client/src/pages/ImageManager.jsx) → identical.
- **Re-proved:** Ran `diff <(sed -n 47,64p client/src/pages/Dashboard.jsx) <(sed -n 506,523p client/src/pages/ImageManager.jsx)` and it printed nothing, so the two Modal components are byte-identical. `plural` is defined identically at Dashboard.jsx:9 and ImageManager.jsx:11. `usesOf` is defined identically at ImageManager.jsx:12-14 and AtlasWorkspace.jsx:2076-2078, and the second copy sits inside `function ImagePicker` (line 2062). The same filter and sort, `rows.sort((a, b) => ((b.start ?? -Infinity) - (a.sta…

### D012 · Legacy columns nothing reads or writes meaningfully: worlds.settings, maps.description/parent_map_id/zoom_level/map_order, links.kind/time_context, images.tags, folder color/icon

Dead code · low · effort m · found by `schema-data` (+2 other lanes)

- **Where:** maps.description is copied by undo's insertMap AND by the world clone (atlas.js:199-201 `INSERT INTO maps (title, description, …)`), so it is not 'only copied by undo's insertMap'. The clone also copies images.tags (atlas.js:190-193). The imageFolders.js color/icon destructure is at line 72, not 73.
- **Files:** `server/config/schema.sql:36`, `server/config/schema.sql:80-90`, `server/config/schema.sql:102`, `server/config/schema.sql:142-152`, `server/config/schema.sql:69`, `server/config/schema.sql:52-53`, `server/routes/atlas.js:262`, `server/routes/atlas.js:586-590`, `server/routes/worlds.js:36`, `server/routes/images.js:36-40`, `server/routes/imageFolders.js:73`
- **What happens:** worlds.settings: POST /worlds writes it from the body and GET returns it, but the client never reads .settings; all prod rows are '{}'. maps.description (0 non-null), parent_map_id (all NULL, with an index idx_maps_parent that has 0 scans), zoom_level (all 1) and map_order (all 0) are only copied by undo's insertMap. /worlds/:id/maps also aliases a COMPUTED value AS parent_map_id (atlas.js:262), which collides confusingly with the dead column. links.time_context is plumbed through POST, clone, undo and two GETs, but the client never sends or reads it (0 non-null rows). links.kind is always 'reference' (86/86); the schema comment promises 'reference or portal' and PATCH accepts any string. images.tags is only ever written as NULL (the client passes '') and the ?tags= filter is never used (0 non-null). image_folders.color and icon are written with defaults and never shown.
- **Why it matters:** Columns in the schema are ones the product uses.
- **Fix:** Remove the plumbing first: settings from worlds.js (lines 36, 85, 107, 131, 143); time_context and kind from atlas.js (227-228, 319-321, 413-415, 728-734, 741) and the client link shape; tags from images.js (36-40, 155, 191), image-base64.js (11, 71) and imageServiceBase64.js (17, 32, 53); color and icon from imageFolders.js. Rename the computed alias at atlas.js:262 to parent_of_owner or similar. Then add `ALTER TABLE … DROP COLUMN IF EXISTS …` statements and `DROP INDEX IF EXISTS idx_maps_parent` to schema.sql, and remove those columns from undo's insertMap.
- **Repro:** Client greps: `grep -rnE "\.settings\b" client/src` → 0; `grep -rn "time_context\|timeContext" client/src e2e server/test` → 0; `grep -rnE "\.color\b|\.icon\b" client/src` → 0; tags only in imageServiceBase64.js plumbing, whose callers pass ''. Prod: links_timectx=0, links.kind all 'reference', worlds_settings≠{}=0, maps_desc=0, img_tags=0, folders=0; pg_stat_user_indexes idx_maps_parent idx_scan=0.
- **Evidence:** grep outputs and prod SELECT counts quoted in repro
- **Re-proved:** Checked each column. worlds.settings: worlds.js:36, 85, 107, 128/131 and 143 write or return it; `grep -rn settings client/src` finds only an unrelated comment, and Dashboard createWorld sends only {name, description}. links.time_context: `grep -rn 'time_context|timeContext' client/src e2e server/test` finds nothing, while atlas.js:227-228 (clone), 319-321, 413-415 and 728-734 plumb it. links.kind: every writer uses 'reference' (atlas.js:728 default, contract.js:403 literal, clone copy); PATCH … _(partly — the corrected location is used above)_
- **Also found as:** "Legacy columns no current code writes, still selected, copied and indexed" (server-dead); "Columns the API still reads and writes but no UI uses: link kind and time conte…" (api-contract)

### O023 · worlds/maps.is_active is a soft-delete leftover: never set to false, yet filtered 29 times; the DELETE route is still labelled '(soft delete)'

Obsolete · low · effort s · found by `server-dead` (+2 other lanes)

- **Where:** The 29 grep hits are 25 SQL filters plus share.js:90-91 (select and JS check) plus atlas.js:587 and 590. Those last two are undo's insertMap copying m.is_active into the INSERT, and they must be removed along with the column or undo of an interior delete will fail after the DROP COLUMN. Add server/routes/atlas.js:586-590 to the fix's file list.
- **Files:** `server/routes/worlds.js:154`, `server/routes/worlds.js:169`, `server/routes/atlas.js:18`, `server/routes/share.js:22`, `server/config/schema.sql:35`, `server/config/schema.sql:88`
- **What happens:** Worlds have been hard-deleted since edd180f (2026-07-11) and maps since the legacy removal in a140976 (2026-08-17). No code writes is_active=false. Every ownership check, list, clone, template and share query still filters `is_active = true`. worlds.js:154 labels the route 'Delete world (soft delete)' directly above a hard DELETE. Production may still hold pre-July soft-deleted rows that these filters hide, together with their images and R2 objects.
- **Why it matters:** A flag nothing sets does not exist, and a comment does not contradict the code under it.
- **Fix:** Via the DB, count `SELECT count(*) FROM worlds WHERE NOT is_active` and the same for maps. Hard-delete those legacy rows (and deletePrefix their R2 keys), then remove the filters and add `ALTER TABLE worlds/maps DROP COLUMN IF EXISTS is_active` to schema.sql. Fix the worlds.js:154 comment now either way.
- **Repro:** grep -rn "is_active" server/routes server/forge | wc -l -> 29. grep -rn "is_active\s*=\s*false\|SET is_active" server (excluding node_modules) -> no hits. git log -S "SET is_active = false" -> last removal a140976.
- **Re-proved:** `grep -rn is_active server/routes server/forge | wc -l` gives 29, but not all 29 are filters. 25 are SQL `is_active = true` filters. share.js:90-91 SELECTs is_active and checks `!m.is_active` in JS. atlas.js:587 and 590 are the undo insertMap re-inserting `m.is_active` from the tombstone snapshot, which is a write of the old value. Searching server (excluding node_modules) for `is_active = false`, `SET is_active` and `is_active=false` found nothing. git log -S 'is_active = false' shows edd180f … _(partly — the corrected location is used above)_
- **Also found as:** "worlds.is_active / maps.is_active are a dead soft-delete: never set false, but …" (schema-data); "Delete-world code and comments describe a soft delete that no longer exists; ev…" (dashboard)

### O025 · 'imageServiceBase64' is really the whole image client, one of its comments points at a file that was deleted, and its upload progress numbers are made up

Obsolete · low · effort s · found by `client-dead`

- **Where:** The rename touches 2 importers, not 3: client/src/pages/ImageManager.jsx:5 and client/src/pages/AtlasWorkspace.jsx:5.
- **Files:** `client/src/services/imageServiceBase64.js:3`, `client/src/services/imageServiceBase64.js:19-38`, `client/src/services/imageServiceBase64.js:46`, `client/src/pages/ImageManager.jsx:133`, `client/src/pages/ImageManager.jsx:396-400`
- **What happens:** Only upload goes to /api/images-base64. List, update and delete go to /api/images, yet the service is named after base64. The comment '(reuse from regular image service)' refers to services/imageService.js, which was deleted in b855a2b. onProgress reports hard-coded 10 → 30 → 60 → 100 around a single awaited POST, so the Archive's upload bar (ImageManager 396-400) sits at 60% for the whole transfer and then jumps to done.
- **Why it matters:** The name should match the job, comments should point at code that exists, and the progress bar should show real progress.
- **Fix:** Rename it to imageService.js (3 importers: AtlasWorkspace, ImageManager). Drop the stale comment. Pass axios `onUploadProgress` to http.post and forward real percentages, or show an indeterminate bar.
- **Repro:** git log --diff-filter=D --name-only -- client/src/services → b855a2b deleted imageService.js. Read imageServiceBase64.js:19-38.
- **Re-proved:** imageServiceBase64.js:3 sets API_BASE='/api/images-base64', which only uploadImage uses (line 36). getImages, getImage, updateImage and deleteImage all hit /api/images (60/70/80/90). The comment at line 46 reads '(reuse from regular image service)', and git log --diff-filter=D shows client/src/services/imageService.js was deleted in b855a2b. onProgress is hard-coded 10, 30, 60, 100 around one awaited http.post with no onUploadProgress (lines 19-38). ImageManager:133 feeds pct into the bar at 39… _(partly — the corrected location is used above)_

